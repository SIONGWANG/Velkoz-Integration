# 审视之眼 项目更新维护日志

---

## 2026-06-09

### UI优化 — 快捷标签位置上移

- **改动内容**：
  1. **`zone_c.py` `render_control_panel`**：快捷标签从备注表单下方移至最终结果（合格/不合格）与备注之间
- **影响范围**：`zone_c.py`
- **备注**：优化常用操作区布局，最终结果 → 快捷标签 → 备注+保存/提交 三项紧凑排列，减少滚动

---

## 2026-06-09

### Bug修复 — V1.2 StreamlitDuplicateElementId 修复

- **改动内容**：
  1. **`zone_b.py` `render_completion_panel`**：`st.download_button` 添加 `key="dl_excel_completion"`
  2. **`export_mixin.py` `render_export_panel`**：`st.download_button` 添加 `key="dl_excel_export_panel"`
- **影响范围**：`zone_b.py`、`export_mixin.py`
- **备注**：两个 `download_button` 标签相同且无 `key` 参数，Streamlit 生成重复元素 ID 导致崩溃。Tag: `v1.2`

---

## 2026-06-09

### Bug修复 — V1.1 导出 Excel 备注/标签列为空

- **改动内容**：
  1. **`data_mixin.py` `_get_df`**：CSV 列名归一化，旧版 `错误反馈` 列自动映射为 `备注`，缺失的 `备注`/`标签` 列自动补空串
  2. **`data_mixin.py` `_sync_tags_to_notes`**：优化哈希检测逻辑，备注为空时允许标签同步（修复空备注阻断标签写入）
  3. **`data_mixin.py` `sync_state_from_history`**：修正 `_manual_edit_` 标记逻辑，备注为空时重置标记，避免空备注被误判为手动编辑
  4. **`data_mixin.py` `save_to_disk`**：保存时增加标签兜底，备注为空但有已选标签时自动写入备注栏
- **影响范围**：`data_mixin.py`（4 处修改）
- **备注**：根因为旧版 CSV 列名不兼容 + 标签同步时序问题，导致导出 Excel 时备注/标签列为空。Tag: `v1.1`

---

## 2026-06-02

### 新增功能 — V1.3.7 CSV 历史文件选择器

- **改动内容**：
  1. **新增 `_scan_existing_csvs()` 方法**：扫描 `_质检记录/{operator}/` 下所有 `*_验收记录.csv`，按日期倒序返回
  2. **修改 `get_csv_filename()`**：优先使用 `st.session_state.selected_csv`（如果设置了且文件存在），否则走原有日期逻辑
  3. **`reset_task_state()` 清除 `selected_csv`**：重置任务时回到默认"今天"模式
  4. **侧边栏添加 CSV 文件选择器**：下拉框选项为"📅 今天（自动）"+ 历史 CSV 文件名
- **影响范围**：`data_mixin.py`、`settings_mixin.py`
- **备注**：解决跨天后旧数据不可见问题，用户无需改系统时间即可查看历史验收记录。

---

## 2026-05-28 15:30

### 规则变更 — V1.3.7 OpenSpec审查归档

- **改动内容**：
  1. **归档完成**：V1.3.7打包发布审查已完成，所有任务已执行
  2. **文档归档**：OpenSpec文档已归档至 `openspec/archive/v1.3.7-packaging-completed-2026-05-28/`
- **影响范围**：`openspec/` 目录
- **备注**：打包产物 `Vel'Koz_V1.3.7.zip`（123MB）已生成，含内嵌Python 3.11.9 + 预装依赖。

---

## 2026-05-28 15:00

### 新增功能 — V1.3.7 打包发布（Vel'Koz 绿色版）

- **改动内容**：
  1. **创建 `requirements.txt`**：依赖清单（streamlit, pandas, Pillow, XlsxWriter, streamlit-paste-button）
  2. **创建 `启动.bat`**：面向用户的启动脚本，首次运行自动初始化内嵌 Python 环境，自动检测可用端口
  3. **创建 `setup_embedded_python.bat`**：开发者工具，下载 Python 3.11.9 embeddable + 安装 pip + 安装依赖
  4. **创建 `打包.bat`**：开发者工具，一键复制文件并生成 Vel'Koz_V1.3.7.zip 发布包
  5. **更新 OpenSpec 文档**：更新 .openspec.yaml 状态为 completed，重写 proposal.md 和 tasks.md
- **影响范围**：项目根目录（新增 4 个文件）、`openspec/changes/v1.3.7-packaging/`（更新 3 个文件）
- **备注**：采用 Python embeddable 绿色版策略，用户解压后双击"启动.bat"即可运行，无需安装 Python 或任何依赖。英文名 Vel'Koz。

---

## 2026-05-28 13:30

### 规则变更 — V1.3.6 OpenSpec审查归档

- **改动内容**：
  1. **审查完成**：V1.3.6鲁棒性优化审查已完成，所有5个问题已修复
  2. **文档归档**：OpenSpec文档已归档至 `openspec/archive/v1.3.6-robustness-optimization-completed-2026-05-28/`
  3. **索引创建**：创建 `openspec/README.md` 索引文件，记录审查流程和版本对应关系
  4. **状态更新**：OpenSpec状态已更新为completed
- **影响范围**：`openspec/` 目录
- **备注**：审查结论：V1.3.6版本鲁棒性优化已完成，所有5个问题已修复，用户体验提升。

---

## 2026-05-28 13:00

### 逻辑修改 — V1.3.6 鲁棒性优化

- **改动内容**：
  1. **编码降级策略优化**：`_get_df` 方法在编码降级时记录警告日志，并在UI上提示用户文件编码异常
  2. **标签同步边界场景优化**：`_sync_tags_to_notes` 方法增加用户输入内容哈希比对，更精确检测手动编辑
  3. **预加载异常处理优化**：`preload_next_images` 方法改为 `logging.warning` 级别，可选在UI上显示预加载状态
  4. **QA报告错误处理优化**：`load_qa_report` 方法在读取失败时记录警告日志，并在UI上提示用户
  5. **配置文件错误处理统一**：`_load_tags_from_disk` 和 `_load_settings` 方法统一错误处理策略，在UI上提示用户配置文件损坏
- **影响范围**：`data_mixin.py`、`disk_io.py`、`settings_mixin.py`、`app.py`
- **备注**：基于V1.3.5鲁棒性审查结果优化，OpenSpec文档见 `openspec/changes/v1.3.6-robustness-optimization/`。优化目标：静默失败 → 明确提示，让用户知道发生了什么。

---

## 2026-05-28 10:00

### 规则变更 — V1.3.5 鲁棒性验证审查

- **改动内容**：
  1. **数据持久化验证**：确认CSV原子写入、编码一致性（utf-8-sig）、重试机制（5次）完整可靠
  2. **前端状态联动验证**：确认session_state管理规范，标签同步逻辑已改进（手动编辑检测机制有效）
  3. **错误处理验证**：确认异常捕获完整，但部分场景提示不够明确（编码降级、QA报告加载失败）
  4. **边界场景验证**：确认大部分场景已覆盖，UNC路径处理和损坏文件处理待加强
  5. **内存管理验证**：确认缓存已有上限控制（100MB/50条），LRU淘汰机制有效
  6. **架构稳定性验证**：确认Mixin依赖清晰，无循环导入，MRO顺序正确
  7. **历史问题回归**：V1.1.2审查的13个问题已修复11个（修复率85%），V1.2.5.2审查的6个P0问题全部修复
  8. **新发现问题**：识别6个新问题（P1:2个, P2:3个, P3:1个），已创建修复任务清单
- **影响范围**：全模块（9个文件），无代码变更，纯审查验证
- **备注**：OpenSpec审查文档见 `openspec/changes/v1.3.5-robustness-review/`。审查结论：V1.3.5版本鲁棒性良好，核心功能稳定可靠。

---

## 2026-05-27 18:00

### 逻辑修改 — V1.3.5 app.py 单体拆分重构（Mixin 多继承架构）

- **改动内容**：
  1. **Phase 1**：提取 CSS/JS 常量至 `css_styles.py`（`MAIN_CSS` + `STATUS_BUTTON_JS`）
  2. **Phase 2**：提取 7 个模块级缓存函数至 `disk_io.py`（`scan_files_from_disk`、`get_display_image_bytes` 等）
  3. **Phase 3**：提取 `DataMixin` 至 `data_mixin.py`（23 个方法：CSV 读写、导航、扫描等）
  4. **Phase 4**：提取 `SettingsMixin` 至 `settings_mixin.py`（19 个方法：设置/配置/标签管理等）
  5. **Phase 5**：提取 `ExportMixin` 至 `export_mixin.py`（10 个方法：导出/归集/弹窗，含 3 个 @st.dialog）
  6. **Phase 6**：提取 Zone Mixins：
     - `zone_a.py` → `ZoneAMixin`（`render_a_zone`，含分页、筛选、列表）
     - `zone_b.py` → `ZoneBMixin`（冷启动动画、确认面板、图片展示、完成面板）
     - `zone_c.py` → `ZoneCMixin`（控制面板：信息/截图/导航/分类/状态/备注/标签）
  7. **Phase 7**：清理未使用导入，消除 `_handle_scanning()` 死代码（run() 内联逻辑改为委托调用），最终 `app.py` 从 3,034 行缩减至 208 行
- **影响范围**：`app.py`（从单文件 3,034 行拆分为 9 个模块）、新增 `css_styles.py`、`disk_io.py`、`data_mixin.py`、`settings_mixin.py`、`export_mixin.py`、`zone_a.py`、`zone_b.py`、`zone_c.py`
- **备注**：采用 Mixin 多继承架构，`AcceptanceApp(DataMixin, SettingsMixin, ExportMixin, ZoneAMixin, ZoneBMixin, ZoneCMixin)`。OpenSpec 审查通过，所有跨 Mixin 依赖经 MRO 验证正确。OpenSpec 文档见 `openspec/changes/v1.3.5-refactor/`。原始备份为 `app_v1.3.5_backup.py`。

---

## 2026-05-27 19:00

### 逻辑修改 — V1.3.5 重构验证报告

- **改动内容**：逐项验证拆分前后行为一致性，结论为**零行为差异**：
  1. 删除的 4 个 `session_state` 键（`error_screenshots`、`uploaded_file_tokens`、`generated_excel_path`、`is_navigating`）均为死代码，仅初始化/reset，从未读取
  2. `_handle_scanning()` 委托逻辑与原始 run() 内联 34 行逐行一致
  3. 所有导入迁移无遗漏，从 app.py 移除的符号均已迁入实际使用模块
  4. `STATUS_OPTIONS` / `HAS_PASTE_LIB` 移至 `zone_c.py`，仅在 `render_control_panel()` 中使用
  5. CSS/JS 内容（`MAIN_CSS` + `STATUS_BUTTON_JS`）与原始内联内容逐字节一致
  6. 61 个方法全部保留，无丢失、无重复定义
- **影响范围**：无代码变更，纯验证
- **备注**：残留死代码 `_fsync_path`（定义但从未调用，因 Windows 文件锁 bug 被弃用）存在于两个版本中，不影响功能

---

## 2026-05-27 01:00

### Bug修复 — 导出合格数据时"系统找不到指定的路径"错误

- **改动内容**：在 `export_qualifies_with_check` 函数中添加源路径存在性检查（`os.path.exists(src_path)`），与 `export_utils.py` 中 `copy_qualified_folders` 行为对齐。源文件夹不存在时跳过并记录，不再抛出 `[WinError 3]` 异常。
- **影响范围**：`app.py`（`export_qualifies_with_check` 函数，约第 1397-1472 行）
- **备注**：根因为扫描缓存与导出之间的时间差导致路径失效，缓存路径是"快照"非实时状态

---

## 2026-05-27 10:00

### 新增功能 — V1.3.1 统一输出文件命名规范

- **改动内容**：
  1. **启动界面新增任务类型选择**：新增"新标"/"返修"单选按钮，全局生效
  2. **操作员姓名改为必填**：加载文件夹前必须填写操作员姓名
  3. **统一命名模板**：所有输出文件采用 `{标签}_{操作员}_{日期}_验收记录` 格式
  4. **新增 `get_output_prefix()` 函数**：返回 `{标签}_{操作员}_{日期}` 前缀
  5. **新增 `get_evidence_folder_name()` 函数**：返回 `_00_Evidence_{标签}`
  6. **修改 `get_csv_filename()`**：CSV 命名改为 `{标签}_{操作员}_{日期}_验收记录.csv`
  7. **修改 `export_excel_with_images()`**：Excel 命名改为 `{标签}_{操作员}_{日期}_验收记录.xlsx`
  8. **修改 `export_qualifies_with_check()`**：合格导出文件夹改为 `{标签}_{操作员}_{日期}_合格数据_{数量}组`
  9. **修改 `export_reject_data()`**：不合格归集文件夹改为 `{标签}_{操作员}_{日期}_不合格_{数量}组`
  10. **截图证据文件夹按标签分离**：`_00_Evidence_新标/` / `_00_Evidence_返修/`
  11. **扫描逻辑更新**：跳过所有 `_00_Evidence_*` 文件夹
- **影响范围**：`app.py`（启动界面、命名函数、导出函数、扫描逻辑）
- **备注**：V1.3.0 复制为 V1.3.1 版本后修改，源版本 1.3.0 未改动

---

## 2026-05-26 22:00

### 新增功能 — 目录层级新增批次（Batch）区分

- **改动内容**：
  1. **`scan_files_from_disk` 支持批次文件夹检测**：扫描时自动识别 `batch_YYYYMMDD_NNN_keyword` 格式的中间目录，将其作为批次标记，批次信息附加到每个图片组的 `group["batch"]` 字段中
  2. **新增 `_parse_batch_name` 函数**：解析批次文件夹名，提取日期、序号、关键词
  3. **新增 `ensure_batch_metadata` 函数**：确保批次目录下存在 `batch_meta.json` 元数据文件，包含批次名、日期、序号、关键词、图片数量、状态、创建时间
  4. **`extract_user_name` 跳过批次文件夹**：路径中的 `batch_*` 段不再被误识别为标注员姓名（特别是当关键词包含中文时）
- **影响范围**：`app.py`（`scan_files_from_disk` 函数 + 新增 2 个辅助函数）、`utils.py`（`extract_user_name` 函数）
- **备注**：向后兼容——无批次文件夹的目录结构不受影响，`group["batch"]` 为可选字段（仅在存在批次时附加）。目标磁盘结构：`项目/标签/batch_YYYYMMDD_NNN_keyword/数字文件夹/图片文件`

---

## 2026-05-26 18:00

### Bug修复 — _save_df 同句柄 fsync 消除 Windows 文件自锁

- **改动内容**：
  1. **`_save_df` 流式写入 + 同句柄 fsync**：`pd.to_csv(tmp_file)` 改为 `with open(tmp_file) as f` + `df.to_csv(f)` + `f.flush()` + `os.fsync(f.fileno())`，写入和 fsync 全程同一文件句柄，消除 `pd.to_csv` 关闭文件后立即 `os.open(O_RDONLY)` 重新打开的竞争窗口（Windows NTFS 句柄释放延迟 / 杀毒软件异步扫描持锁）
  2. **`_save_df` 重试增强**：`os.replace` 重试从 3 次/100-300ms 改为 5 次/300-1500ms，适配 Windows 文件同步盘/杀毒软件的持锁时长
  3. **`_save_df` 异常清理加固**：`os.remove(tmp_file)` 加 `except OSError` 防止连锁失败
  4. **`_fsync_path` fd 泄漏修复**：`try/except` 改为 `try/finally`，确保 `os.open` 成功后 `os.close` 一定执行（当前 `_save_df` 已不调用此方法，但修复供其他场景安全使用）
- **影响范围**：`app.py` `_save_df` 方法（L382-417）、`_fsync_path` 方法（L427-439）
- **备注**：此问题在 5/21 曾通过移除 fsync 修复，但 5/26 12:00 V1.2.5.2 openspec 审查重新引入了 fsync 调用，导致问题复现。本次用同句柄 fsync 方案彻底解决——既保留 fsync 数据落盘保障，又消除二次打开文件的竞争窗口。

---

## 2026-05-26 15:00

### 功能删除 — V1.2.5.3 删除抽检复核功能，保留抽检验收

- **改动内容**：删除"抽检复核"（inspection_mode）功能，该功能是从已验收 CSV 中抽样比对原结果的质检审计工具。删除 5 个专属方法（get_inspection_csv_path、save_inspection_result、_save_df_at、start_inspection、_build_group_from_path）、7 个 session_state 变量初始化、render_inspection_panel 中的复核配置区和状态显示，以及散布在 14 个位置的 inspection_mode 条件分支。保留"抽检验收"（sampling_acceptance_mode）功能不变。
- **影响范围**：`app.py`（删除 360 行，3259→2899 行）
- **备注**：从 V1.2.5.2 复制为 V1.2.5.3 版本后修改，源版本 1.2.5.2 未改动。AST 语法检查通过，grep 验证 inspection_mode/inspection_df/_inspection_data_groups 引用均为 0，sampling_acceptance 引用保留 14 处。

## 2026-05-26 15:30

### 配置变更 — 修正程序名称为"审视之眼pro"

- **改动内容**：冷启动动画标题和浏览器标签页标题从"审视之眼"修正为"审视之眼pro"，与目录结构一致
- **影响范围**：`app.py`（行 777 冷启动标题、行 2625 set_page_config）
- **备注**：修正之前版本遗留的名称不一致问题

## 2026-05-26 12:00

### Bug修复 / 逻辑修改 — V1.2.5.2 质检记录保存可靠性与前端状态联动修复（6项）

基于 V1.2.5.1 深层审查（openspec），修复 P0 共 6 项问题：

- **改动内容**：
  1. **[P0] pd.read_csv encoding 统一**：全局 9 处 pd.read_csv 调用补上 encoding='utf-8-sig'，避免 Windows 环境下 GBK 乱码
  2. **[P0] 截图保存失败不中断**：save_to_disk 返回值从 bool 改为 (bool, str)，收集失败截图索引，保存完后统一提示；调用方 render_control_panel 根据返回值显示不同 toast
  3. **[P0] CSV 读取失败提示升级**：_get_df 中文件存在但读取失败时，st.warning 升级为 st.error，附带具体错误详情和操作建议（关闭占用程序、刷新重试）
  4. **[P0] _save_df 加 fsync**：to_csv 之后、os.replace 之前加 self._fsync_path(tmp_file)，确保数据落盘
  5. **[P0] _save_df_at 补备份逻辑**：补上 os.replace(path, bak_file) 备份 + tmp 清理 try/except，与 _save_df 行为对齐
  6. **[P0] pills 缓存清除**：sync_state_from_history 恢复标签后清除 freq_pills_{id} 和 other_pills_{id}；reset_task_state 的 prefixes 中补上 freq_pills_ 和 other_pills_
- **影响范围**：`app.py`（9处 encoding 修改 + 4处逻辑修改）
- **备注**：从 V1.2.5.1 复制为 V1.2.5.2 版本后修复，源版本 1.2.5.1 未改动

---

## 2026-05-26 09:30

### Bug修复 / 逻辑修改 — V1.2.5.1 审查问题修复（14项）

基于 V1.2.5 全量代码审查（openspec），修复 P0–P2 共 14 项问题：

- **改动内容**：
  1. **[P0] 扫描缓存击穿修复**：`_cache_buster` 改为 `cache_buster`（去掉下划线前缀），使其参与 `@st.cache_data` 缓存键计算，重新加载文件夹可正确刷新
  2. **[P0] 抽检验收进度计数修复**：只统计采样子集 ID 内的已完成条数，不再读取全量 CSV 行数
  3. **[P1] 状态按钮位置调整**：合格/不合格/修改后合格/待定按钮移到备注框上方，用户无需滚动即可完成"选状态→填备注→提交"流程
  4. **[P1] "不合格"目录跳过逻辑修复**：从 `if "不合格" in root` 改为 `if os.path.basename(root) == "不合格"`，根路径含"不合格"时不再误跳过
  5. **[P1] 裸 except 替换**：抽检复核/抽检验收面板的 `except: pass` 改为 `except Exception as e: logging.warning(...)`
  6. **[P1] LRU 图片缓存感知文件变更**：`_load_and_rotate` 加入 `_mtime` 参数作为缓存键，替换图片后刷新页面可见新图
  7. **[P1] `_sr_working` 状态清理**：`reset_task_state` 中添加 `pop('_sr_working')`，切换任务后不再残留旧规则
  8. **[P1] 归集安全机制**：`collect_reject_folders` 从 `shutil.move` 改为 `copytree` + `rmtree`，复制失败时源数据不丢失
  9. **[P2] Excel 图片动态缩放**：根据图片实际宽度计算缩放比例（目标 150px），小图不再不可见
  10. **[P2] 筛选完成行为优化**：筛选条件下最后一条提交后跳回筛选列表首条，不触发全量完成面板
  11. **[P2] 历史备注保护**：`sync_state_from_history` 恢复记录时，备注非空且与标签不同则设置 `manual_edit=True`，防止标签同步覆盖手写备注
  12. **[P2] fallback 图片标记**：`_build_group_from_path` 返回 `fallback_image` 字段，B 区显示"图片未按规则匹配"警告
  13. **[P2] 导航预加载修正**：`go_to_previous/next_group` 预加载使用筛选后的索引，不再预加载全量列表中的下一条
  14. **[P2] 快捷键匹配优化**：视图按钮改为按出现顺序匹配（排除已标记按钮），增加容错
- **影响范围**：`app.py`（12处修改）、`utils.py`（1处修改）、`export_utils.py`（1处修改）
- **备注**：从 V1.2.5 复制为 V1.2.5.1 版本后修复，源版本 1.2.5 未改动

---

## 2026-05-26 00:10

### UI/UX 优化 — V1.2.5 视觉设计改进

- **改动内容**：
  1. **间距松绑**：列间距 1px→4px，页面底部 0→1rem，metric 内边距 4px 6px→8px 12px，分隔线间距 0.15rem→0.3rem，按钮 padding 4px 8px→5px 10px
  2. **状态按钮颜色区分**：合格=淡绿底(#dcfce7)、不合格=淡红底(#fee2e2)、修改后合格=淡蓝底(#dbeafe)、待定=淡黄底(#fef9c3)；选中态对应加深为实色；通过 JS 自动识别按钮文本并注入 data-status 属性
  3. **冷启动标题**：font-weight 300→400（中文不会太细），letter-spacing 6px→3px（中文不需要大间距），副标题颜色 #999→#666（对比度从 2.8:1 提升到 5.7:1）
  4. **全屏按钮 hover 颜色**：#ff4b4b(红)→#6366f1(主色调蓝紫)，保持视觉一致
  5. **滚动条恢复**：从完全隐藏改为 4px 细滚动条（半透明，hover 时加深），用户知道能滚
- **影响范围**：`app.py` — `run()` CSS 块、冷启动动画 CSS、全屏按钮 CSS
- **备注**：基于 V1.2.4 UI/UX 审查的 5 项视觉改进，版本号递增为 V1.2.5

---

## 2026-05-25 23:50

### Bug修复 — V1.2.4 代码审查 8 项 P1 问题修复

- **改动内容**：
  1. **Issue 1+2 — 手动编辑检测逻辑修复**：`_manual_edit_` 标志不再要求 `_last_tags_` 非空才生效；清空备注时自动重置标志位为 False
  2. **Issue 4 — Excel 图片插入失败静默吞掉**：`except: pass` 改为 `except Exception: failed_images.append(...)`，导出完成后在返回消息中报告失败图片数量
  3. **Issue 5 — CSV 存在但读取为空的静默失败**：`_get_df` 中增加检测，CSV 文件存在但 DataFrame 为空时显示 `st.warning` 提示用户检查文件占用
  4. **Issue 3 — 导出确认面板无取消按钮**：`show_export_confirm` 区域增加「❌ 取消」按钮，可关闭确认面板
  5. **Issue 6 — 不合格数量读取异常吞掉**：`show_reject_confirm_dialog` 中 `except: pass` 改为 `except Exception as e: logging.warning(...)`
  6. **Issue 7 — 标签库保存失败无用户反馈**：`_save_tags_to_disk` 异常时增加 `st.warning` 提示
  7. **Issue 8 — 设置保存失败无用户反馈**：`_save_settings` 异常时增加 `st.warning` 提示
- **影响范围**：`app.py` 的 `_get_df`、`_save_tags_to_disk`、`_save_settings`、`show_reject_confirm_dialog`、`export_excel_with_images`、`render_export_panel`
- **备注**：基于 V1.2.3 代码审查发现的 8 项 P1 问题，全部在新版本目录 1.2.4 中修复，源版本 1.2.3 未改动

---

## 2026-05-25 23:30

### Bug修复 — 导出后按钮不可交互

- **改动内容**：
  1. **修复 Excel 导出后下载按钮消失**：`st.balloons()` 触发 rerun 后，`st.button()` 返回 False，导致下载按钮和打开文件夹按钮不再渲染。改为将 `file_path` 存入 `st.session_state._excel_export_path`，rerun 后仍显示下载按钮
  2. **修复一键导出结果消失**：导出成功/失败后 `st.balloons()` 触发 rerun，结果信息丢失。改为存入 `st.session_state._export_result`，持久化显示
  3. **修复确认导出后结果消失**：同上，确认导出后结果也持久化
  4. **完成面板同步修复**：`render_completion_panel` 中的 Excel 导出同样改为 session_state 持久化
  5. **添加「清除结果」按钮**：用户可手动清除导出结果和报表状态
  6. **`reset_task_state` 清理**：切换任务时清除 `_export_result`、`_excel_export_path`、`show_export_confirm`
- **影响范围**：`app.py` 的 `render_export_panel`、`render_completion_panel`、`reset_task_state`
- **备注**：从 V1.2.2 的日志追加（V1.2.3 已有设置面板统一的改动）

---

## 2026-05-25 23:15

### 逻辑修改 — V1.2.3 设置面板统一

- **改动内容**：
  1. **提取 `_render_settings_panel()`**：将活跃状态和冷启动状态的「更多设置」面板统一为一个方法，通过参数控制差异：
     - `show_operator=True` 显示操作员名称输入（冷启动）
     - `show_datasource=True` 显示数据源加载器（活跃状态）
     - `key_prefix="cs_"` 冷启动布局滑块 key 前缀
  2. **消除重复代码**：操作员名称输入、视图切换、扫描规则、快捷键面板不再重复书写
- **影响范围**：`app.py` 的 `run()` 方法、新增 `_render_settings_panel()` 方法
- **备注**：从 V1.2.2 复制为 V1.2.3 版本后修改

---

## 2026-05-25 23:00

### 逻辑修改 — V1.2.2 run() 方法瘦身

- **改动内容**：
  1. **提取 `render_export_panel()`**：导出管理 UI（一键导出、确认流程、归集、Excel 报表）从 `run()` 中抽出为独立方法，减少 ~88 行
  2. **提取 `_render_view_mode_toggle()`**：视图模式切换（四宫格/单图对比）原先在设置面板和冷启动设置中各写一遍，现统一为一个方法
  3. **提取 `_render_hotkeys_section()`**：快捷键开关 + 说明面板同样重复出现两次，现统一为一个方法
- **影响范围**：`app.py` 的 `run()` 方法、新增 3 个方法
- **预期效果**：
  - `run()` 从 ~436 行降到 ~301 行（减少 31%）
  - 消除视图切换和快捷键面板的代码重复
  - 每个方法职责单一，可独立理解和修改
- **备注**：从 V1.2.1 复制为 V1.2.2 版本后修改

---

## 2026-05-25 22:30

### 逻辑修改 — V1.2.1 快捷键稳定性重构

- **改动内容**：
  1. **快捷键改用 `data-hotkey` 属性绑定**：原先快捷键通过 `innerText.includes("⬅️ 上一条")` 等文本匹配定位按钮，按钮文案一旦改动快捷键就失效。改为：先用文本匹配给按钮打 `data-hotkey` 属性标签，后续所有快捷键通过 `document.querySelector('[data-hotkey="..."]')` 精确查找
  2. **标签持久化**：`data-hotkey` 属性写入 DOM 后不会因 Streamlit 局部重渲染而丢失，只有按钮元素被替换时才需要重新标记
  3. **容错增强**：文本匹配规则增加模糊匹配（如 `"上一条"` 不依赖 emoji），降低按钮文案变化导致快捷键失效的风险
- **影响范围**：`app.py` 的 `inject_hotkeys` 方法
- **预期效果**：快捷键稳定性显著提升，不再因按钮文案微调而失效
- **备注**：从 V1.2.0 复制为 V1.2.1 版本后修改

---

## 2026-05-25 22:00

### 新增功能 — V1.2.0 提交反馈 + 导出模块抽离

- **改动内容**：
  1. **提交成功反馈**：「仅保存」后显示 `st.toast("✓ 已保存")`，「提交并下一条」后显示 `st.toast("✓ 已提交")`，用户有明确的操作确认
  2. **导出模块抽离**：新建 `export_utils.py`，将纯计算逻辑从 `app.py` 中提取为独立函数：
     - `get_missing_ids(all_loaded_ids, csv_file)` — 查找未保存的 ID
     - `collect_reject_folders(reject_df, target_dir)` — 归集不合格文件夹
     - `copy_qualified_folders(qualified_df, target_dir, id_to_group)` — 复制合格文件夹（含逐文件校验）
     - `verify_exported_data(expected_ids, export_dir, id_to_group, df)` — 导出后验证
  3. **`app.py` 对应瘦身**：`_get_missing_ids`、`export_reject_data`、`verify_exported_data` 改为调用 `export_utils` 中的函数，UI 层（`st.status`、`st.dialog`、`st.progress`）保留在 `app.py`
- **影响范围**：`app.py` 的 `render_control_panel` 提交逻辑、`_get_missing_ids`、`export_reject_data`、`verify_exported_data`；新增 `export_utils.py`
- **备注**：从 V1.1.7.2 复制为 V1.2.0 版本后修改

---

## 2026-05-25 21:00

### 逻辑修改 — V1.1.7.2 缓存策略统一

- **改动内容**：
  1. **移除 `get_record_status_map` 的 `@st.cache_data` 装饰器**：该函数原先使用两层缓存（`@st.cache_data` + `_get_df` 的 `st.session_state`），导致每次数据变更后需要手动调用 `.clear()` 清除外层缓存，且 7 处调用点存在遗漏风险。移除后统一依赖 `_get_df` 的 `st.session_state` 缓存，单一数据源
  2. **移除全部 7 处 `get_record_status_map.clear()` 调用**：`save_to_disk`、`save_inspection_result`、`start_inspection`、`start_sampling_acceptance`、退出抽检验收、退出抽检复核、扫描完成
  3. **简化 `_save_df`**：移除冗余的缓存更新逻辑（原先写入缓存后被 `save_to_disk` 立即 pop，白费功夫），`_save_df` 只负责原子写入文件
  4. **统一 `get_record_status_map` 默认参数**：`csv_path` 默认值从 `get_csv_filename()` 改为 `_get_active_csv_path()`，与所有调用方行为一致
- **影响范围**：`app.py` 的 `get_record_status_map`、`_save_df`、`save_to_disk`、`save_inspection_result`、`start_inspection`、`start_sampling_acceptance`、侧边栏退出按钮、扫描完成处理
- **预期效果**：
  - 数据保存后，`get_record_status_map` 自动返回最新数据（因为 `_get_df` 读取的 `st.session_state` 缓存已被 `_save_df_at` 更新，或被 `save_to_disk` pop 后从磁盘重读）
  - 消除两层缓存不一致的风险
  - 减少 7 处手动缓存清除的维护负担
- **备注**：从 V1.1.7.1 复制为 V1.1.7.2 版本后修改

---

## 2026-05-25 20:00

### 逻辑修改 — V1.1.7.1 消除闪屏 + 快捷键帮助

- **改动内容**：
  1. **A 区添加 `@st.fragment`**：`render_a_zone` 用 `@st.fragment` 包裹，筛选按钮切换时只刷新 A 区列表和统计，B/C 区不重渲染
  2. **B 区添加 `@st.fragment`**：`render_b_image_area` 用 `@st.fragment` 包裹，B 区内部交互只刷新 B 区
  3. **A 区选中 ID 改用全局刷新**：radio 列表选中不同 ID 时调用 `st.rerun(scope="app")`，确保 B/C 区同步更新
  4. **快捷键帮助面板**：在快捷键开关下方添加可展开的快捷键映射表，用户随时查看按键说明
- **影响范围**：`app.py` 的 `render_a_zone`、`render_b_image_area` 方法，快捷键设置区域
- **预期效果**：
  - 点标签 → 只有 C 区刷新，A/B 区不动
  - 切换筛选 → 只有 A 区刷新，B/C 区不动
  - 切换数据/提交 → 三区全部刷新（正确行为）
  - 快捷键帮助随时可查
- **备注**：从 V1.1.7 复制为 V1.1.7.1 版本后修改

---

## 2026-05-25 19:00

### 逻辑修改 — V1.1.7 系统审查与鲁棒性修复

- **改动内容**：
  1. **修复冷启动标题版本号字面量**：`render_cold_start_animation` else 分支添加 `f` 前缀，`{version}` 正确替换
  2. **修复 page_title 硬编码**：`st.set_page_config` 的 `page_title` 改用动态版本号 `os.path.basename(BASE_DIR)`
  3. **标签同步竞态修复**：`render_tag_selector` 添加 `@st.fragment` 装饰器，标签选择变化只重渲染标签区域，不触发全局 rerun
  4. **状态重置完整性**：`reset_task_state` 的 `prefixes` 添加 `"_manual_edit_"` 和 `"_pending_tag_sync_"`，切换任务时清除残留标记
  5. **异常处理健壮性**：`preload_next_images`、`_get_missing_ids` 的裸 `except` 改为 `except Exception` + 日志；`_save_settings` 添加 try/except
  6. **CSV 编码一致性**：`_get_missing_ids` 的 `pd.read_csv` 添加 `encoding='utf-8-sig'`
  7. **备份策略统一**：`_save_df_at` 不再删除 `.bak` 备份文件，与 `_save_df` 行为一致
  8. **扫描规则空后缀过滤**：图片/文本槽位解析过滤空字符串，避免误匹配
- **影响范围**：`app.py` 多个方法（冷启动、标签选择、状态管理、异常处理、导出、扫描规则）
- **备注**：从 V1.1.6 复制为 V1.1.7 版本后修复，聚焦功能和交互鲁棒性

---

## 2026-05-25 18:00

### Bug修复 — 全局数据 ID 去重

- **改动内容**：修复所有从 `groups` 构建 `all_ids` 的位置未去重的问题。当 `data_groups` 或 `_inspection_data_groups` 包含重复 ID 时（如 CSV 中存在重复行、或目录扫描产生同名文件夹），会导致列表重复、统计数据不准、导航索引错误。统一改用 `list(dict.fromkeys(...))` 去重且保持顺序。
- **影响范围**：`app.py` 的以下 7 处：
  1. `_get_filtered_ids` — 筛选列表去重
  2. `go_to_prev_group` — 上一条导航索引
  3. `go_to_next_group` — 下一条导航索引
  4. `render_a_zone` — 统计面板总数修正（`len(groups)` → `len(all_ids)`）
  5. `render_control_panel` — 提交后跳转导航
  6. `render_completion_panel` — 完成面板统计
- **备注**：用户反馈筛选"合格"后同一数据出现两次，根因是 `_inspection_data_groups` 从 CSV 构建时未去重。此为同类模式的系统性修复。

---

## 2026-05-25 17:30

### Bug修复 — V1.1.5 P0 问题修复（数据保存与导出）

- **改动内容**：
  1. **修复 save_to_disk 读取 CSV 编码不一致**：`pd.read_csv` 添加 `encoding='utf-8-sig'`，与写入编码保持一致，避免 BOM 头导致列名错位
  2. **修复 Excel 导出混入其他批次数据**：`export_excel_with_images` 添加当前数据组过滤，只导出 `all_ids` 范围内的记录
- **影响范围**：`app.py` 的 `save_to_disk` 方法、`export_excel_with_images` 方法
- **备注**：从 V1.1.3 复制为 V1.1.5 版本后修复

---

## 2026-05-25 17:40

### Bug修复 — V1.1.5 P1 问题修复

- **改动内容**：
  1. **缓存内存上限**：`get_display_image_bytes` 增加 100MB 总大小限制，防止大量高分辨率图片占用过多内存
  2. **版本号硬编码**：冷启动动画标题改为动态读取目录名 `V{version}`
  3. **CSV 写入重试机制**：`_save_df` 增加 3 次重试，与 `_save_df_at` 行为一致
  4. **证据图片路径兼容**：Excel 导出时优先从 CSV 所在目录查找证据图片，回退到 BASE_DIR
- **影响范围**：`get_display_image_bytes`、`render_cold_start_animation`、`_save_df`、`export_excel_with_images`
- **备注**：P1 问题全部修复

---

## 2026-05-25 17:08

### Bug修复 — V1.1.3 P0 问题修复

- **改动内容**：
  1. **修复标签同步覆盖备注问题**（P0-1）：`_sync_tags_to_notes` 增加手动编辑检测，用户手动修改备注后禁止自动同步；增加标签-备注分离的安全检查，无法可靠分离时保留原备注
  2. **修复抽检复核提交跳转逻辑**（P0-2）：`render_control_panel` 中提交后跳转改用 `nav_ids`（已筛选列表）替代 `all_ids`（全量列表），避免筛选条件下跳转到错误条目
- **影响范围**：`app.py` 的 `_sync_tags_to_notes` 方法、`render_control_panel` 方法
- **备注**：从 V1.1.2 复制为 V1.1.3 版本后修复，原版本保留不动

---

## 2026-05-25 17:15

### Bug修复 — 统计数据范围错误

- **改动内容**：
  1. 修复 `render_a_zone` 统计面板中，合格/不合格等计数包含了 CSV 中其他批次数据的问题
  2. 修复 `render_completion_panel` 完成面板中同样的统计范围问题
  3. 修复 `export_qualifies_with_check` 中确认导出弹窗的统计范围问题
  4. 修复 `export_qualifies_with_check` 中 `qualified_df` 可能导出其他批次合格数据的问题
- **影响范围**：`app.py` 的 `render_a_zone`、`render_completion_panel`、`export_qualifies_with_check` 方法
- **备注**：用户反馈筛选"全部"显示 4 条合格，筛选"合格"却显示 6 条，根因是 CSV 文件包含多个批次的数据，统计时未限定当前数据组范围

---

## 2026-05-17 初始创建

### 版本创建

- **改动内容**：从 v1.3.1 源码目录复制 app.py 和 DataScope.py 到 1.0.1 目录，准备进行 DataScope → app 功能迁移
- **影响范围**：新建 1.0.1 版本目录
- **备注**：原始文件保留在 源码/ 目录不动

---

## 2026-05-17 21:23

### 新增功能 — DataScope.py → app.py 全功能迁移

- **改动内容**：
  1. 顶部配置区：STATUS_OPTIONS 增加"修改后合格"；新增 DEFAULT_CATEGORIES / CONFIG_FILENAME / get_config_path / load_categories_config / save_categories_config；删除 TASK_RECORDS_FOLDER_NAME；新增 extract_user_name / scan_files_from_disk；删除 find_image_file / scan_files_collect_mode
  2. __init__ 改造：新增 categories_config / sticky_l1 / sticky_l2 / inspection_mode / inspection_df / inspection_source_file / inspection_ratio / inspection_scope / inspection_csv_path / inspection_data_source / inspection_mismatch_reason / sampling_acceptance_mode / sampling_acceptance_ratio / confirm_pending / pending_groups / annotator_map / annotator_confirm_enabled；删除 custom_tags / selected_tags / qa_mode / start_sequence / current_sequence / next_group_action
  3. 设置持久化：_load_settings / _save_settings 增加 annotator_confirm_enabled 字段
  4. 删除旧标签系统：_load_tags_from_disk / _save_tags_to_disk / render_tag_selector / _sync_tags_to_notes
  5. 保存逻辑改造：save_collect_result / _do_save_collect_result → save_to_disk (CSV列: 姓名/图片ID/一级/二级/结果/错误反馈/错误截图/时间/路径)；get_csv_filename 改为任务名直接存根目录
  6. 新增 L1/L2 分类系统：render_category_manager (精简版增删改 + 保存/恢复默认)
  7. 新增标注员确认：render_confirm_panel (按标注员聚合 + 姓名编辑 + 跳过/确认)；A区标注员确认开关 toggle
  8. 新增抽检验收/复核：render_inspection_panel / start_inspection / start_sampling_acceptance / get_inspection_csv_path / save_inspection_result / _build_group_from_path / _get_missing_ids
  9. C区控制面板重建：render_control_panel 整合 L1 pills + L2 pills + 状态 pills + 错误截图 (evidence_pool) + 导航 + 错误反馈 + 保存/提交
  10. B区改造：ZH/EN 双语编辑区 + AI 质检数据源展示 (final_report.csv)
  11. 新增导出：export_reject_data (不合格归集物理移动) / show_reject_confirm_dialog / export_qualified_data
  12. 筛选适配：_get_filtered_ids 增加"修改后合格"；render_a_zone 增加对应 emoji
  13. sync_state_from_history 适配新 CSV 列名 (一级/二级/错误反馈)
  14. run() B区分支：confirm_pending → 确认面板；is_scanning → 加载动画+scan_files_from_disk → annotator_confirm_enabled 控制是否进入确认
  15. 保留 app.py 原有：缓存机制 (_load_and_rotate LRU / get_display_image_bytes)、EXIF 旋转、预加载滑动窗口、冷启动动画、快捷键注入、export_excel_with_images、verify_exported_data、show_missing_data_dialog
- **影响范围**：app.py (1.0.1 版本完整重写，2461行)
- **备注**：DataScope.py 只读不修改，作为功能参考保留

---

## 2026-05-17 21:45

### Bug修复 — 代码审查后修复

- **改动内容**：
  1. export_excel_with_images 证据图片路径修复：`BASE_DIR/error_screenshots/norm_path` → `BASE_DIR/norm_path`（与 save_to_disk 中 rel_path 的保存方式对齐）
  2. render_a_zone 筛选 pills 增加 try/except 回退到 st.radio（与其他 st.pills 调用保持一致）
  3. 删除死代码 render_c_nav_area（导航已整合到 render_control_panel）
- **影响范围**：app.py 第 1590 行、第 1739 行、第 1997-2016 行
- **备注**：代码审查发现 7 个问题，已修复 3 个（2 critical + 1 important），其余为建议性优化

---

## 2026-05-18

### 新增功能 — DataScope_pro.py → app.py 二次迁移 (v1.0.2)

- **改动内容**：
  1. **扫描模式替换**：删除 `extract_user_name`、旧版 `scan_files_from_disk`（扁平结构），替换为嵌套目录扫描（每8位数字文件夹一组），支持 ZH/CH/EN/en 大小写不敏感，跳过"不合格"目录
  2. **新增 `find_image_file()` 工具函数**：按 stem 大小写不敏感匹配图片文件
  3. **`_build_group_from_path` 适配**：同步更新为嵌套结构扫描逻辑（ZH/CH/EN/en）
  4. **标签系统迁入**：`custom_tags` / `selected_tags` session state；`_load_tags_from_disk` / `_save_tags_to_disk`（`config/tags.json` 持久化）；`render_tag_selector`（常用标签+全部标签双组 pills 多选）；`_sync_tags_to_notes`（标签同步到错误反馈）；标签管理 UI（新增/删除/全选/清空）
  5. **`save_to_disk` 增加"标签"列**：保存选中标签到 CSV
  6. **`sync_state_from_history` 增加标签恢复**：从 CSV "标签"列恢复已选标签
  7. **CSV 下载按钮**：设置面板中增加直接下载当前质检记录 CSV 的功能
  8. **导出两阶段确认流程**：一键导出改为先检查未保存数据 → 拦截展示 missing IDs → 用户确认后强制导出
  9. **标题更新**：V1.0.1 → V1.0.2
- **影响范围**：app.py (1.0.2 版本)
- **备注**：数据源从 源码/DataScope_pro.py 迁入

## 2026-05-18 14:30

### 交互优化 — A 区布局重设计

- **改动内容**：
  1. 删除重复进度区（"已完成/剩余" 与 render_a_zone 内指标重复）
  2. A 区从上到下重组：全屏 → 抽检复核 → 指标+筛选+列表 → 导出管理 → AI预识别(折叠) → 更多设置(折叠) → 分类管理(折叠)
  3. 视图切换（四宫格/单图对比）从独立区域移入"更多设置"
  4. 快捷键 toggle 移入"更多设置"
  5. AI 质检数据源折叠为 "AI 预识别" expander
  6. 导出管理（导出合格数据/一键归集/Excel报表）移到 AI 预识别之上
- **影响范围**：app.py run() 方法 A 区块

## 2026-05-18 14:45

### 逻辑优化 — 导出一键化 + CSV下载归位

- **改动内容**：
  1. 导出合格数据改为自动路径：无需手动输入，自动在根目录同级创建 `{任务名}_合格数据_{MMDD}_{N}组`
  2. 一键导出两阶段：先检查未保存 → 拦截 → 确认强制导出（调用 `export_qualifies_with_check`）
  3. CSV 下载从"更多设置"移到"导出管理"
- **影响范围**：app.py run() 导出管理区块、更多设置区块

---

## 2026-05-18 15:00

### 交互优化 — A 区 + C 区布局重设计 (v1.0.3)

- **改动内容**：
  1. **A 区全屏移入更多设置**：全屏按钮从 A 区顶部移除，放入"更多设置"expander 第一项
  2. **A 区抽检复核位置调整**：从 A 区顶部移到 AI 预识别上方（render_a_zone 下方）
  3. **A 区打开文件夹移除**：原"更多设置"内的"打开当前文件夹"按钮移入 C 区顶部
  4. **C 区从上到下重排**：数据信息（👤姓名 + 🆔ID + 📂打开文件夹）→ L1/L2 pills → 最终结果（一排）→ 不一致原因 → 快捷标签 → 错误截图 → 上一条/下一条 → 备注 + 保存/提交 → 标签管理（折叠，最底部）
  5. **C 区字段重命名**：错误反馈 → 备注
  6. **标题更新**：V1.0.2 → V1.0.3
- **影响范围**：app.py render_control_panel 方法、run() 方法 A 区块、更多设置区块

---

## 2026-05-18 15:30

### 细节优化 (v1.0.4)

- **改动内容**：
  1. **标注员确认开关重命名**：`☑️ 启用标注员确认` → `👤 验收组名称预确认`（名称更直观，help 文本保持不变）
  2. **A 区新增"合格率"指标**：r3c2 填入 📊 合格率，公式为 (合格 + 修改后合格) / 总数 × 100%
  3. **最终结果与快捷标签视觉差异化**：最终结果从 `st.pills` 改为 `st.segmented_control`（回退方案 `st.radio(horizontal=True)`），与快捷标签 pills 视觉区分
  4. **错误截图粘贴按钮优化**：列宽比从 [1,1] 改为 [2,1]（按钮更宽不截断），标签从"📋 点击粘贴(Ctrl+V)"缩短为"📋 粘贴 (Ctrl+V)"
- **影响范围**：app.py L2531（checkbox label）、L1779 r3c2（合格率 metric）、L1988-1989（segmented_control）、L2012/L2017（列宽+label）

---

## 2026-05-18 16:30

### 新增功能 — 冷启动界面精简 + L1/L2 自动感知 + 导出确认结构化 (v1.0.5)

- **改动内容**：
  1. **冷启动界面精简（居中卡片式布局）**：冷启动时（current_group 为 None 且非扫描中），A 区和 C 区完全清空，B 区居中渲染启动卡片（动画 + 路径输入 + 加载按钮 + 验收组名称预确认 checkbox，位于 `st.container(border=True)` 中），下方"更多设置"expander 只保留全屏/布局/视图/快捷键（数据源区提升到卡片中）。加载数据后恢复三栏布局不变。
  2. **L1/L2 自动感知**：当用户在"分类管理"中删除所有 L1 分类后，C 区不再显示 L1/L2 pills，提交合格/修改后合格时不再强制要求选择 L1/L2。恢复分类后自动恢复要求。
  3. **导出确认结构化**：废弃此前 `st.warning` 文字提示，改为 inline 结构化指标块：3 列 × 2 行 `st.metric` 网格展示🟢合格/🔴不合格/🔵修改后合格/🟡待定/⚪未检/📦总计，下方 info 说明导出数量，确认按钮改为"✅ 已知悉，确认导出"primary 按钮。
  4. **标题更新**：V1.0.4 → V1.0.5
- **影响范围**：app.py `export_qualifies_with_check`（L1395-1406）、`render_control_panel` L1/L2 pills（L1978-1998）、提交校验（L2073）、`set_page_config` page_title、冷启动动画标题、`run()` A 区（L2420-2570）和冷启动分支（L2639-2691）

---

## 2026-05-18 17:00

### 交互优化 — 标签组布局锁死 + metric 视觉统一 (v1.0.6)

- **改动内容**：
  1. **A 区筛选标签组（3×2 网格锁死）**：`st.pills` 全部/未检/合格/修改后合格/不合格/待定 → CSS `display: grid; grid-template-columns: 1fr 1fr`，3 行 × 2 列等宽排布，灰底圆角 metric 卡片风格（`#f0f2f6` 背景、4px 圆角、8px 字号、居中），选中态 indigo 边框 + 浅紫底（`#e0e7ff` + `#6366f1`），`flex-wrap: nowrap` 永不换行
  2. **C 区状态标签组（2×2 网格锁死）**：`st.segmented_control` 合格/不合格/修改后合格/待定 → CSS 覆写默认 flex 为 `display: grid; grid-template-columns: 1fr 1fr`，2 行 × 2 列，同 metric 卡片风格
  3. **通用布局隔离**：两类标签组均通过 CSS `!important` 覆写 Streamlit 默认布局，不随「更多设置」expander 展开/收缩或页面横向拉伸改变内部排版结构，仅等比缩放适配窗口大小
  4. **标题更新**：V1.0.5 → V1.0.6
- **影响范围**：app.py `<style>` CSS 块 `div[data-testid="stPills"]` + `div[data-testid="stSegmentedControl"]`、`set_page_config` page_title、冷启动动画标题
- **备注**：纯 CSS 方案，Python 逻辑零改动；stPills 全局 2 列网格同时影响 L1/L2/标签/视图等 pills，效果为次要 pills 也变为网格排列

---

## 2026-05-18 17:20

### 逻辑修改 — CSV 归档到质检记录子目录 + Excel 导出打开文件夹

- **改动内容**：
  1. **CSV 归档子目录**：`get_csv_filename()` 返回路径改为 `质检记录/【完成验收】{任务名}.csv` 和 `质检记录/【抽检验收】{任务名}.csv`（原为根目录裸文件名）；`_save_df()` 和 `save_to_disk()` 增加 `os.makedirs(os.path.dirname(csv_file), exist_ok=True)`，确保写入前目录存在
  2. **Excel 导出打开文件夹**：导出成功后，下载按钮旁增加 "📂 打开文件夹" 按钮，调用 `open_in_system(export_dir)` 打开 Excel 所在目录
- **影响范围**：app.py `get_csv_filename` (L893-898)、`_save_df` (L387)、`save_to_disk` (L900)、Excel 导出区 (L2564-2572)
- **备注**：CSV 目录变更全局生效，所有 `get_csv_filename()` 调用者（读取/写入/导出）自动适配；`.tmp` / `.bak` 防错文件同步存入质检记录目录

---

## 2026-05-18 17:30

### Bug修复 — CSV 路径改为绝对路径

- **改动内容**：`get_csv_filename()` 从相对路径 `质检记录/...` 改为 `os.path.join(BASE_DIR, "质检记录", ...)` 绝对路径，与 `error_screenshots` 等其他路径保持一致，避免因 Streamlit CWD 不同导致 CSV 存到意外位置
- **影响范围**：app.py `get_csv_filename` (L893-897)
- **备注**：之前用相对路径，Streamlit 的 CWD 可能不在 app.py 目录，导致用户找不到 CSV 文件

---

## 2026-05-18 17:45

### 架构变更 — 存储路径彻底脱离程序目录，迁移到用户业务路径

- **改动内容**：
  1. **`get_csv_filename()` 路径根切换**：从 `os.path.join(BASE_DIR, "质检记录", ...)` 改为 `os.path.join(root_path, "质检记录", ...)`，CSV 不再与程序本体绑定，存入用户选中的任务目录
  2. **`get_inspection_csv_path()` 同步迁移**：抽检复核 CSV 同步存入 `{root_path}/质检记录/`（原为 CWD 裸文件名）
  3. **`save_inspection_result()` 增加目录创建**：`os.makedirs(os.path.dirname(csv_file), exist_ok=True)`，与 `save_to_disk` / `_save_df` 一致
  4. **写入触发规则**：仅 `save_to_disk` / `_save_df` / `save_inspection_result` 写入时才创建目录，未执行质检不产生任何文件
  5. **公用互通**：同路径下所有人打开工具选中该路径，均可读取同一份 `质检记录/` 下 CSV 数据；不同业务路径相互独立
- **影响范围**：app.py `get_csv_filename` (L893-898)、`get_inspection_csv_path` (L971-974)、`save_inspection_result` (L975-976)、`save_to_disk` (L900-901)、`_save_df` (L387-388)
- **备注**：Excel 报表自动跟随 CSV 存入 `{root_path}/质检记录/`；证据截图 (`_00_Evidence_All`) 保持 `BASE_DIR` 不变

---

## 2026-05-18 18:00

### Bug修复 — C 区 / A 区按钮点击闪动消除

- **改动内容**：移除 `render_control_panel` 状态按钮和 `render_a_zone` 筛选按钮中的 `st.rerun()` 调用，依赖 `@st.fragment` 自身机制自动重绘
- **影响范围**：app.py `render_a_zone` (L1809-1812)、`render_control_panel` (L2021-2024)、`__init__` (L279)、`run()` 保存/提交逻辑 (L2183-2199)、top_banner 展示 (原 L2447-2449)
- **备注**：删除 top_banner_msg 系统（保存成功绿色横幅）消除主闪动源——横幅出现后下次 fragment 重跑时其 DOM 消失导致 C 区整体上移，造成"闪烁+下拉"；恢复 st.rerun() 解决两击选中问题（fragment 读取 current_status 时为旧值）；同时清理 mode_name 无用变量

---

## 2026-05-19 10:00

### Bug修复 — 6 项 UX 缺陷批量修复

- **改动内容**：
  1. **Space 快捷键失效修复**：JS 选择器 `"➡️ 提交并下一条"` → `"提交并下一条"`，去除 emoji 前缀匹配所有三种按钮文本（提交/复核/抽检）
  2. **CSV 原子写入保护**：`save_to_disk` 中 `df.to_csv()` 替换为 `self._save_df(df)`，复用已有 `.tmp` → `.bak` → `replace` 机制，防崩溃损毁
  3. **筛选切换越界保护**：`focus_img_idx` 在 `current_id` 被筛选强制回退到 `filtered_ids[0]` 时间步重置为 0，防止单图视图越界
  4. **证据截图保存后清空**：保存成功后 `st.session_state[pool_key] = []`，避免重复点击"仅保存"时截图反复存盘
  5. **C 区上下条导航补全**：增加 `needs_scroll_top = True` + `preload_next_images()`，与 A 区列表导航行为对齐
  6. **历史状态恢复走缓存**：`sync_state_from_history` 中 `pd.read_csv()` 替换为 `self._get_df()`，利用 session_state 缓存避免每次切 ID 都读磁盘
- **影响范围**：app.py L851 (JS)、L944 (save_to_disk)、L1822-1823 (render_a_zone)、L2184 (保存成功清空)、L2083-2090 (C 区导航)、L456 (sync_state_from_history)
- **备注**：全部低风险改动，均为单点修复不涉及架构变更

---

## 2026-05-19 10:30

### Bug修复 — C 区交互导致 B 区跳动问题

- **改动内容**：`render_control_panel` 和 `render_a_zone` 内所有不涉及页面跳转的交互，从 `st.rerun()` 改为 `on_click` 回调更新 session_state，仅依赖 fragment 自身重绘，避免触发完整页面重跑
  1. **状态按钮**（合格/不合格等）：`st.rerun()` → `on_click=lambda opt: st.session_state.update({'status_pills': opt})`
  2. **筛选按钮**（全部/未检等）：同上模式
  3. **L1 pills**：删除 `st.rerun()`（pills 变更已自动触发 fragment 重跑）
  4. **标签管理按钮**（添加/删除/全选/清空）：删除 `st.rerun()`
  5. **证据截图清空**：`st.rerun()` → `on_click` 回调
  6. **保留 `st.rerun()` 的场景**：上一条/下一条导航、提交并下一条、仅保存（需要刷新 B 区图片）
- **影响范围**：app.py `render_control_panel` (L1964-2180)、`render_a_zone` (L1809-1812)
- **备注**：根因是 `@st.fragment` 内 `st.rerun()` 触发完整页面重跑 → B 区图片 DOM 重建 → 跳动；改为 on_click 后 fragment 仅自身重绘，B 区不刷新

---

## 2026-05-19 11:00

### 架构变更 — 移除所有 @st.fragment，行为统一为全页重跑

- **改动内容**：移除 `render_a_zone` / `render_control_panel` / `render_single_image_view` / `render_b_image_area` 四个方法的 `@st.fragment` 装饰器；保留 `on_click` 回调模式（回调先更新 session_state，全页重跑时读到新值即可正确渲染）
- **影响范围**：app.py L1769、L1962、L2212、L2241
- **备注**：fragment 在此 Streamlit 版本中重跑范围不可控——有时仅 fragment 重跑，有时触发全页重跑，导致"第一次闪、第二次不闪"的不一致体验。去掉后每次全页重跑行为可预期，配合图片缓存 + CSS 固定容器高度，闪动降到最低

---

## 2026-05-19 16:00

### Bug修复 — 闪屏/偏移根因修复 (v1.0.6.1)

- **改动内容**：
  1. **合并 `components.html()` iframe**：`run()` 中两个独立 `components.html()` 调用（滚动置顶脚本 + CSS 变量注入脚本）合并为单个 iframe，减少 rerun 时的 DOM 元素创建/销毁抖动。滚动 JS 仅在 `needs_scroll_top` 为 True 时才注入。
  2. **`render_control_panel` 重新启用 `@st.fragment`**：C 区整体封装为 fragment，状态按钮（合格/不合格/待定/修改后合格）、L1/L2 pills、标签管理、错误截图等所有不涉及页面跳转的交互仅触发 fragment 局部重渲染，B 区图片不被波及。
  3. **导航按钮使用 `st.rerun(scope="app")`**：上一条/下一条、提交并下一条在 fragment 内显式调用 `st.rerun(scope="app")`（带 `TypeError` 兼容回退到 `st.rerun()`），精确定义需要全页跳转的场景。单次全页 rerun 替代原来"按钮 click 触发一次 + 显式 `st.rerun()` 再触发一次"的双重渲染。
  4. **"仅保存"不触发额外 rerun**：表单提交自然触发 fragment 重渲染，证据池清空后直接展示，无需第二次 rerun。
- **影响范围**：app.py `run()` L2440-2460（合并 iframe）、`render_control_panel` L1961（`@st.fragment` 装饰器）、L2091-2109（导航按钮 `scope="app"`）、L2192-2216（表单提交分流逻辑）
- **备注**：与 v1.0.6 11:00 版（移除所有 fragment）的关键差异——之前 fragment 内混用 `st.rerun()` 导致重跑范围不可控（有时 fragment 有时全页）。本版仅在需要页面跳转时显式 `st.rerun(scope="app")`，其余交互全部依赖 fragment 自然重跑，行为可预期。合并 iframe 消除"页面顶部插入空格又删除"的 DOM 抖动。如果 `scope="app"` 参数在当前 Streamlit 版本不可用，代码自动回退到 `st.rerun()`（效果等同于单次全页重跑）。

---

## 2026-05-19 16:30

### Bug修复 — B 区进度条截断 + 纵向间距压缩

- **改动内容**：
  1. **进度条高度修复**：`components.html(height=22)` → `height=28`，内层 div 增加 `overflow:visible`，消除进度条被裁切问题
  2. **CSS 纵向间距压缩（5 条规则）**：
     - 全局 `hr` margin 从默认 ~1em 压缩到 `0.15rem`
     - B区 `stCaptionContainer`（分辨率说明行）margin 清零
     - B区 `stNotification`（AI 质检通过/未通过提示框）padding 从默认 ~1rem 压缩到 `0.15rem 0.4rem`
     - B区 `textarea` margin-bottom 负值压缩
     - B区 `stExpander`（查看完整 JSON 数据折叠面板）margin 清零
  3. **ZH/EN 文本框高度缩减**：`st.text_area(height=80)` → `height=60`，释放 40px 纵向空间
- **影响范围**：app.py `render_b_image_area` L2259-2266（进度条 iframe）、L2318/L2321（ZH/EN textarea）、`<style>` 块 L2443-2448（5 条 CSS 规则）
- **备注**：所有 CSS 规则通过 `:nth-of-type(2)` 限定 B 区列，不影响 A 区和 C 区布局。纵向空间释放后 AI 预识别表格不再被挤压遮挡

---

## 2026-05-19 17:30

### 新增功能 — 整批验收完成面板 + C 区锁定 (v1.0.6.2)

- **改动内容**：
  1. **B 区完成面板**：新增 `render_completion_panel()` 方法，最后一条提交成功后 B 区切换为完成面板：balloons 庆祝动画 + 8 项 metric 统计卡片（合格/不合格/修改后合格/待定/未检/合格率/总计）+ CSV 存盘路径 + 4 个操作按钮（返回第一条、导出 Excel、打开 CSV 目录、归集不合格/待定）
  2. **C 区锁定**：`render_control_panel` 检测到 `_batch_completed` flag 后提前返回，仅显示 "✅ 本轮验收已完成，C 区已锁定" 提示，不渲染任何控件，杜绝误操作
  3. **A 区可复查**：统计面板和列表照常运作，点击列表条目或分页按钮自动清除 flag 并跳转，恢复到正常验收界面
  4. **`_batch_completed` flag 机制**：`__init__` 初始化为 False；最后一条提交成功时设为 True + `st.rerun(scope="app")` 触发全页更新；A 区列表点击/分页跳转/返回第一条按钮清除 flag
  5. **三种模式统一覆盖**：正常验收、抽检验收、复核模式均走同一套完成面板逻辑
- **影响范围**：app.py `__init__` L282（flag 初始化）、`render_control_panel` L1966-1968（C 区提前返回）、L2219-2223（最后一条设 flag + rerun）、新增 `render_completion_panel` L2225-2287（B 区完成面板）、`run()` L2839-2843（B 区分支检测）、`render_a_zone` L1863/L1898（A 区清除 flag）
- **备注**：解决之前最后一条提交后仅一行 `st.success()` 极易被忽略的问题，用户现在无法错过 balloons + 完整统计面板。C 区完全锁定避免已完成状态下误改数据

---

## 2026-05-19 18:00

### 逻辑修改 — CSV 列名统一 + Excel 导出打开文件夹修复 + 标题更新 (v1.0.7)

- **改动内容**：
  1. **CSV 列名统一"备注"**：
     - `save_to_disk` 写入 record：`"错误反馈"` → `"备注"`，删除 `"标签"` 列（标签已通过 `_sync_tags_to_notes` 合并到备注文本中，不单独成列）
     - `save_inspection_result` 写入 record：`"复核备注"` → `"备注"`
     - 两份 CSV（完成验收 + 抽检验收）列名完全统一为 9 列：`姓名 / 图片ID / 一级 / 二级 / 结果 / 备注 / 错误截图 / 时间 / 路径`
  2. **向后兼容读取**：`sync_state_from_history` 两处历史记录读取使用 `row.get('备注', row.get('错误反馈', ''))` 兜底，旧 CSV 仍可正常回显
  3. **标签恢复兼容**：`sync_state_from_history` 中标签恢复逻辑对无 `"标签"` 列的新格式 CSV 自动跳过（`row.get('标签', '')` 返回空字符串 → 兜底为空列表）
  4. **完成面板 Excel 导出增强**：导出按钮旁增加"⬇️ 下载 Excel"download_button + "📂 打开文件夹"按钮，解决此前仅内联打开文件夹按钮不可用的问题
  5. **`_sync_tags_to_notes` 文档更新**：docstring 描述从"错误反馈"改为"备注栏"
  6. **标题更新**：V1.0.6 → V1.0.7
- **影响范围**：app.py `save_to_disk` L928-939（record dict）、`save_inspection_result` L997-1010（record dict）、`sync_state_from_history` L437-491（两处兜底读取）、`_sync_tags_to_notes` L407（docstring）、完成面板 Excel 导出 L2278-2293（download + open folder 按钮）、L238（flag 初始化）、L518（reset 清除）、L731/L2434（标题）
- **备注**：读路径全部使用 `dict.get` 兜底，不依赖 CSV 列顺序，旧格式 CSV 和新格式 CSV 均可正常读取。标签列移除后，标签文本已在备注中跟随 `; ` 分隔符保留，不影响数据完整性

---

## 2026-05-19 18:30

### 新增功能 — 标注员确认流程三项 UX 改进 (v1.0.8)

- **改动内容**：
  1. **Tooltip 文本扩展**：`?` 悬浮提示从简短描述"加载后先确认标注员姓名再开始验收"扩展为详细说明，解释功能适用场景（父级总文件夹、多标注员子文件夹、统一命名规范、同步录入验收报表）
  2. **选择性标注员过滤**：确认面板中每个标注员卡片新增"纳入"checkbox，默认全选；取消勾选的标注员数据不进入 A 区列表；确认按钮显示过滤后条数；跳过确认按钮不受影响（仍加载全部标注员）
  3. **加载反馈**：点击"确认并开始验收"后增加 `st.spinner("正在启动验收，数据加载中...")` 加载动画
  4. **A 区列表标注员姓名显示**：列表项在 8 位 ID 后显示 `· 张三` 格式的标注员姓名（未知标注员不显示）
  5. **确认面板标题优化**：从"检测到标注员，请确认"改为"检测到负极目录下有多个子标注员项，请确认它们的名称"
  6. **标题更新**：V1.0.7 → V1.0.8
- **影响范围**：app.py `__init__`（`annotator_inclusion` session state）、`reset_task_state`（重置 inclusion）、`run()` 扫描完成块（初始化 inclusion）、两处 tooltip help 文本、`render_confirm_panel`（卡片布局 + 过滤逻辑 + spinner）、`render_a_zone`（`_fmt` 函数增加姓名显示）、`set_page_config` page_title、冷启动动画标题
- **备注**：过滤在改名之前执行（`annotator_inclusion` key 为原始名），避免用户将"未知"改名后过滤失效。跳过确认按钮行为不变，始终加载全部标注员

---

## 2026-05-19 20:00

### Bug修复 + 逻辑修改 — 抽检复核模式深度优化 (v1.0.8)

- **改动内容**：
  1. **修复 A 区列表数据丢失（核心 Bug）**：新增 `_saved_data_groups` / `_saved_current_id` 原始数据快照机制；进入抽检验收/复核模式前保存原始全集；退出时恢复原始数据；消除此前 `data_groups = []` 直接清空导致数据不可恢复的问题
  2. **复核模式 data_groups 隔离**：`run()` 中复核模式数据存入独立 `_inspection_data_groups`，不再覆盖 `data_groups`；`render_a_zone()` / `run()` current_group 查找 / `get_processed_ids()` / `_get_filtered_ids()` 根据 `inspection_mode` 自动选择数据源
  3. **CSV 路径显示保护**：`root_path` 在抽检流程中无意外写入；`get_csv_filename()` 仅前缀区分模式（【完成验收】/【抽检验收】），任务名不变
  4. **删除上传 CSV 入口**：`render_inspection_panel()` 移除数据源选择（pills/radio）和上传 CSV 面板（file_uploader + 重复代码块）；`start_inspection()` 移除 `source == "上传CSV"` 分支，固定从当前 CSV 读取；`__init__` 移除 `inspection_csv_path` / `inspection_data_source` 初始化
  5. **文件命名统一**：`get_inspection_csv_path()` → `抽检复核记录.csv`（固定命名，与常规质检 CSV 清晰区分）；`export_excel_with_images()` 复核模式下导出为 `抽检复核记录.xlsx`；质检记录目录统一加 `-` 前缀 → `-质检记录`，利用系统排序规则自动置顶文件夹列表最顶端
  6. **模式互斥保证**：进入抽检验收前自动退出抽检复核（恢复快照 + 清理状态），反之亦然
  7. **B 区导航适配**：上一条/下一条导航按钮、`render_completion_panel`、`render_b_image_area` 进度条均根据 `inspection_mode` 使用 `_inspection_data_groups`
  8. **缓存一致性**：`load_record_df` 在复核模式下读取复核 CSV；模式切换时清除 `get_record_status_map` 缓存
- **影响范围**：app.py `__init__`（新增 3 个 session state 字段，移除 2 个）、`start_inspection`（快照 + 移除上传 CSV 分支 + 互斥）、`start_sampling_acceptance`（快照 + 互斥）、退出按钮（恢复快照）、`run()`（_inspection_data_groups 隔离 + lookup_groups）、`render_a_zone`（groups 选择 + id_to_group）、`get_inspection_csv_path`（固定命名）、`export_excel_with_images`（复核模式命名）、`render_inspection_panel`（简化面板）、`get_record_status_map`（清除缓存）、`load_record_df`（复核模式路径）、`_get_df`（csv_file 参数）、`get_processed_ids` / `_get_filtered_ids`（groups 选择）、B 区导航/`render_completion_panel`/`render_b_image_area`（groups 适配）
- **备注**：抽检复核模式与正常验收模式现在可无缝切换，不丢数据，命名体系统一

---

## 2026-05-19 20:30

### 定稿调整 — CSV 命名简化 + 目录前缀 + 加载按钮修复 + C 区重排

- **改动内容**：
  1. **CSV 抽检分支下线**：`get_csv_filename()` 移除 `sampling_acceptance_mode` 分支，统一只用 `【完成验收】{task_name}.csv`，不再产出 `【抽检验收】` 变体
  2. **复核文件命名**：`抽检验收 - 抽检复核表.csv` → `抽检复核记录.csv`，Excel 同步改为 `抽检复核记录.xlsx`，与常规质检 CSV 清晰区分
  3. **质检记录目录前缀**：`质检记录/` → `-质检记录/`，利用 `-` 排序规则在文件夹列表中自动置顶
  4. **加载按钮永久置灰修复**：`run()` 中新增 `is_scanning` 处理块（在 `confirm_pending` 检查后、`current_group` 检查前），数据已加载时点击"加载文件夹"不再卡死在 disabled 状态，正确执行重新扫描 + 清理所有模式状态
  5. **C 区布局重排（从上至下）**：错误截图 → 快捷标签 → 上一条/下一条 → 备注 + 仅保存/提交并下一条 → 不一致原因（复核模式）→ 状态按钮（合格/不合格/修改后合格/待定）→ 标签管理
  6. **按钮样式统一**：`仅保存` 和 `提交并下一条` 均使用 `type="primary"`，消除高低错落视觉差
- **影响范围**：app.py `get_csv_filename`（移除抽检分支）、`get_inspection_csv_path`（命名）、`export_excel_with_images`（命名）、所有 `"质检记录"` 字符串（4 处 → `"-质检记录"`）、`run()`（新增 is_scanning 处理块 L2807）、`render_control_panel`（L2063-2177 整体重排）

---

## 2026-05-20 当前

### 新增功能 + 交互优化 (v1.0.9)

- **改动内容**：
  1. **A 区筛选联动 B 区定位**：A 区筛选切换「全部/未检/合格/修改后合格/不合格/待定」时，列表通过 JS `scrollIntoView({behavior: 'smooth', block: 'nearest'})` 自动滚动到当前选中项的 radio 按钮，确保筛选结果始终可见
  2. **C 区快捷标签位置调整**：快捷标签模块（`render_tag_selector`）从原"错误截图之后、导航按钮之前"移动到"最终结果按钮之后、标签管理 expander 之前"，与标签管理模块紧邻，便于打完标签后直接保存
  3. **底部按钮样式 & 布局优化**：备注栏下方「仅保存」和「提交并下一条」按钮列宽比从 `[1, 1]` 改为 `[1, 2]`，"仅保存"按钮缩短、"提交并下一条"按钮拉长，视觉主次分明
  4. **C 区四大主标签文字强化**：合格、不合格、待定、修改后合格 4 个核心状态按钮通过 CSS（`font-weight: 900; font-family: "SimHei", "黑体", "Microsoft YaHei", sans-serif`）设置为加粗/黑体，突出主操作按钮视觉层级
  5. **标题更新**：V1.0.8 → V1.0.9
- **影响范围**：app.py `render_a_zone`（筛选联动 JS 滚动块）、`render_control_panel`（快捷标签位置移动 + 底部按钮列宽比 + 状态按钮 CSS 包裹）、冷启动动画标题

---

## 2026-05-20 15:00

### 性能优化 — P0 优化：扫描缓存 + 布局 slider 减 rerun (v1.1.0)

- **改动内容**：
  1. **目录扫描结果缓存**：`scan_files_from_disk()` 添加 `@st.cache_data(show_spinner=False)` 装饰器，新增 `_cache_buster` 参数（默认 0）；每次用户显式点击"加载文件夹"时递增 `st.session_state._scan_cache_buster` 并传入，强制刷新缓存；`render_inspection_panel` 中的调用传入当前 cache buster 以命中缓存。重复加载同一路径时直接返回缓存结果，避免 `os.walk` 全量扫描
  2. **布局 slider 消除重复 rerun**：4 处布局占比 slider（A 区"更多设置"×2 + 冷启动"更多设置"×2）从"值变更时立即 `st.rerun()`"改为"仅更新 session_state + 持久化设置 + 新增「应用布局」按钮触发 rerun"。slider 拖动期间 Streamlit 自身会触发一次 rerun（widget 机制），之前代码中额外 `st.rerun()` 导致每次拖动产生双重全页重渲染。现在改为按钮手动应用，拖动无额外开销
- **影响范围**：app.py `scan_files_from_disk`（函数签名 + `@st.cache_data`）、`__init__`（新增 `_scan_cache_buster` session state 初始化）、`render_inspection_panel`（缓存命中调用）、`run()` 扫描处理块（递增 buster）、"更多设置" expander 内布局 slider 块（移除 `st.rerun()` + 添加 `key=` + 添加"应用布局"按钮）×2 处
- **备注**：P0-1 扫描缓存对大型数据集（数百组以上）重复加载提速 10x+；P0-2 消除布局 slider 拖拽时的双重渲染闪烁

---

## 2026-05-20 15:30

### 性能优化 — P1 优化：CSV 查找 O(1) + 图片预加载异步 + 导出 st.status

- **改动内容**：
  1. **CSV 查找 O(1) 字典化**：新增 `_build_lookup(df)` 方法构建 `图片ID → 行记录` 映射字典；`_get_df()` 和 `_save_df()` 同步维护 `_df_lookup_{path}` 缓存；`_save_df_at()` 同样构建查找字典（抽检复核路径）；新增 `_get_record_by_id(current_id)` 方法统一收口（抽检复核模式走 inspection_df，正常模式走 O(1) 字典查找）；`sync_state_from_history()` 从"每切一张图都 `df.copy()` + `df[df['图片ID']==id]` 全量扫描"改为单次 `_get_record_by_id()` 调用；移除 `save_to_disk` 和 `save_inspection_result` 中冗余的 `st.session_state.pop(cache_key)`（`_save_df` / `_save_df_at` 已写入最新缓存，pop 导致下次 `_get_df` 重新读盘）
  2. **图片预加载异步化**：`preload_next_images()` 从同步阻塞改为 `threading.Thread(daemon=True)` 后台线程，仅预热 `_load_and_rotate` 的 LRU 缓存（线程安全），不再阻塞 UI 线程。磁盘 I/O + PIL 解码在后台完成，用户切图时图片已在内存中
  3. **导出进度改用 `st.status`**：`export_qualifies_with_check()` 从 `st.progress()` + `st.empty().markdown()` 逐条更新改为 `with st.status(...)` 上下文管理器 + `status.update(label=...)`；每 3 条或最后一条更新状态文本，减少 UI 刷新频率；完成时调用 `state="complete"` / `state="error"` 标记终态。`export_reject_data()` 同样改用 `st.status`
- **影响范围**：app.py `_build_lookup`（新增）、`_get_df`（构建查找字典）、`_save_df`（同步构建）、`_save_df_at`（同步构建 + 补全 `os.remove(bak_file)`）、`_get_record_by_id`（新增收口方法）、`sync_state_from_history`（简化为 1 行查找）、`save_to_disk`（删冗余 pop + 删 `_df_cache_` 清理注释）、`save_inspection_result`（删冗余 pop）、`preload_next_images`（改为 daemon 线程）、`export_qualifies_with_check`（st.status 重构）、`export_reject_data`（st.status 重构）、顶部新增 `import threading`
- **备注**：P1-1 将切图延迟降低约 50%（消除 `df.copy()` + O(n) 扫描，1000 行 CSV 从毫秒级降到微秒级）；P1-2 已回退（见下方）；P1-3 导出进度 UI 更新频率降低 3x + 视觉更整洁

---

## 2026-05-20 15:45

### 回退 — P1-2 异步预加载回退为同步

- **改动内容**：`preload_next_images()` 从后台 daemon 线程异步方案回退为原始同步预加载（同时预热 LRU + display 缓存、`import threading` 移除）。回退原因：异步方案只预热 LRU 缓存不预热 display 缓存，下张图仍需缩放编码；快速连续切图会创建多个线程抢占磁盘；同步确定性更高，保证下张图两个缓存都命中
- **影响范围**：app.py `preload_next_images`（恢复同步逻辑）、顶部移除 `import threading`
- **备注**：P1 最终保留两项：CSV O(1) 查找 + 导出 st.status

---

## 2026-05-20 16:00

### 优化 — P2 改进：session_state 清理 + CSV fsync + 模块拆分

- **改动内容**：
  1. **切换到新数据集时清理旧 session_state keys**：在 `__init__` 的 `reset_task_state()` 中将 `"_df_lookup_"` 前缀 keys 纳入清理范围，避免旧数据集的内存缓存污染新数据集
  2. **CSV 原子写入加 fsync 确保持久化**：新增 `_fsync_path()` 静态方法，每次 CSV 写入（`_save_df()`、`_save_df_at()`）后对临时文件做 `os.fsync()` + 对目录做 `os.fsync()` 确保数据落盘；所有 `pd.read_csv(csv_file, dtype=str)` 统一改为 `encoding='utf-8-sig'`（3 处：`save_to_disk`、`save_inspection_result`、`start_inspection`），避免中文 Windows 系统 GBK 编码乱码
  3. **模块拆分**：新建 `utils.py` 独立模块，抽取所有不依赖 Streamlit 状态的纯工具函数和常量（`is_supported_image`、`find_image_file`、`_load_and_rotate`、`auto_rotate_image`、`resize_image_for_display`、`get_config_path`、`load_categories_config`、`save_categories_config`、`read_txt` 及 `BASE_DIR`、`IMAGE_EXTENSIONS`、`PRELOAD_AHEAD`、`MAX_DISPLAY_PX`、`DISPLAY_JPEG_QUALITY`、`DEFAULT_CATEGORIES`、`CONFIG_FILENAME`、`EVIDENCE_FOLDER_NAME` 常量）；app.py 通过 `from utils import (...)` 导入；删除 app.py 中重复的函数定义和常量声明；`ImageFile.LOAD_TRUNCATED_IMAGES = True` 移入 utils.py；移除 `from functools import lru_cache`（已移至 utils.py）；类内 `self.read_txt()` 调用改为直接调用 `read_txt()`
- **影响范围**：新建 `utils.py`；app.py 删除 6 个重复函数定义 + 2 个常量 + 1 个 import；3 处 `self.read_txt()` → `read_txt()`；`save_to_disk`、`save_inspection_result`、`start_inspection` 中 `pd.read_csv` 统一编码
- **备注**：P2 三项全部完成。模块拆分减少 app.py 约 60 行冗余代码，utils.py 可独立单元测试

---

## 2026-05-20 16:20

### Bug修复 — P3-D `_get_df` 重试空操作 + P3-K `_build_group_from_path` 无图兜底产 `[None]`

- **改动内容**：
  1. **`_get_df` 重试编码降级**：`pd.read_csv` 首次尝试 `utf-8-sig`，失败后改为 `gbk` 编码重试（之前两次调用完全相同的参数，重试无意义）。兼容旧版本或非标准环境下可能产生的 GBK 编码 CSV
  2. **`_build_group_from_path` 兜底路径完善**：当 `find_image_file` 找不到匹配图片、走 fallback 捡图后，给 `has_image1` 补设 `True`（原始终 `False`）；如果 fallback 也找不到任何图片则直接返回 `None`（之前会产出 `images: [None]`，下游拿到空列表导致异常）
- **影响范围**：`_get_df`（编码降级重试）、`_build_group_from_path`（兜底逻辑加固）
- **备注**：P3-D 避免了旧编码 CSV 读取失败时直接返回空 DataFrame 丢数据；P3-K 避免了不合规范文件夹导致程序崩

---

## 2026-05-20 17:00

### 优化 — P3 全面收尾：session_state 清理 + 死代码删除 + UI 去重 + 日志完善 + 多处微优化

- **改动内容**：
  1. **P3-J 切换数据集时清理残余 key**：`reset_task_state()` 新增清理 `show_export_confirm`、`_pending_export_stats`、`annotator_map`，避免旧数据集状态泄漏到新数据集
  2. **P3-C 删除死代码**：移除类内 `read_txt(self, path)` 方法（`utils.read_txt` 已导入且全部调用已统一为直接引用）
  3. **P3-A 提取 `_rerun_app()` 静态方法**：4 处 `try: st.rerun(scope="app") / except TypeError: st.rerun()` 统一收口为 `self._rerun_app()`，消除重复代码
  4. **P3-F 审查模式 group 构建去重**：`run()` 中审查模式 group 重建加 `_inspection_data_groups is None` 守卫，只在首次或 rescan 时构建，不再每帧 rebuild
  5. **P3-I 内部函数提升为类方法**：`_del_tag` 和 `_clear_pool` 从 `render_control_panel` 内部函数改为 `_del_tag(self, ...)` 和 `_clear_evidence_pool(self, pool_key)` 类方法，不再每次 fragment 重渲染时重新创建
  6. **P3-G/H UI 块去重**：提取 `_render_layout_sliders(key_prefix="")` 和 `_render_datasource_loader()` 两个方法，消除"更多设置"展开面板与冷启动页中布局滑块 + 数据源加载器的重复代码（共减少 ~40 行重复）
  7. **P3-E/L 异常日志完善**：`_fsync_path` 失败时写入 `logging.warning`；所有 `st.pills` / `st.radio` 降级 fallback 的 `except:` 改为 `except Exception:`（不吞 KeyboardInterrupt/SystemExit）
- **影响范围**：`reset_task_state`（新增 3 个 key 清理）、`_rerun_app`（新增静态方法，替换 4 处调用点）、`_del_tag` + `_clear_evidence_pool`（新增类方法）、`_render_layout_sliders` + `_render_datasource_loader`（新增方法，替换 2 处调用点）、`run()`（审查 group 构建加守卫、冷启动 UI 改用新方法）、`render_control_panel`（删除内部函数 + 调整调用）、`_fsync_path`（加 warning 日志）、5 处 `st.pills` fallback（bare except → except Exception）、删除 `read_txt` 类方法
- **备注**：P3 全部收尾完成。app.py 相比 v1.0.9 原始版本净减少约 120 行冗余/重复代码，模块结构更清晰

---

## 2026-05-20 17:20

### 逻辑修改 — 「验收组名称预确认」姓名提取规则升级

- **改动内容**：`scan_files_from_disk` 中标注员姓名提取从"取相对路径第一段"改为智能识别：
  1. 从路径末端向上逐段查找
  2. 跳过 8 位纯数字段（数据文件夹 ID）
  3. 跳过已知状态/分类标签（合格、不合格、待定、修改后合格、未检）
  4. 匹配以 2-3 个中文字符开头的段（允许后面跟后缀如返修、新标等，如"张三返修"→ 识别为"张三返修"）
  5. 找不到则回退"未知"
- **影响范围**：`utils.py`（新增 `extract_user_name` 函数 + `_SKIP_SEGMENTS` 常量 + `import re`）、`app.py`（`scan_files_from_disk` 调用新函数、import 新增 `extract_user_name`）
- **备注**：解决了扁平目录（8 位数文件夹直接放在根目录）和多层嵌套（合格/不合格等中间目录）场景下姓名被错误识别为 8 位数字 ID 或状态标签的问题

---

## 2026-05-21 11:00

### Bug修复 — 4 个运行时 Bug + 5 个边缘情况

- **改动内容**：
  1. **Bug #1：标签删除按钮崩溃**：`render_control_panel` 中 `_del_tag` 从内部函数提升为类方法后，`on_click` 回调漏改，`_del_tag(...)` → `self._del_tag(...)`
  2. **Bug #2：审查/复核模式空白**：`start_inspection` 将 `_inspection_data_groups` 初始化为 `[]`，与 `run()` 中的 `is None` 守卫冲突，导致 lazy-build 永远不触发。改为 `= None` 统一
  3. **Bug #3：TXT 指令保存非原子**：`save_to_disk` 中 ZH/EN 指令 TXT 文件直接覆盖写入，改为 `.tmp` + `os.replace()` 原子写入
  4. **Bug #4：`_save_df_at` 数据丢失窗口**：使用 `os.rename`（Windows 非原子），两次 rename 之间崩溃导致 CSV 丢失。改用 `os.replace`（全平台原子）
  5. **Edge #5：上一条/下一条无视 A 区筛选**：C 区导航按钮改用 `_get_filtered_ids()` 获取筛选后的 ID 列表，筛选"未检"时点下一条不再跳到"合格"
  6. **Edge #9：确认面板验证标注名时跳过未纳入的标注员**：不再因已取消勾选的标注员名称为空而阻止确认
  7. **Edge #10：`_save_tags_to_disk` 加 try/except**：磁盘满或权限问题时写日志而非崩溃
  8. **Edge #11：`load_categories_config` 损坏配置加日志告警**：JSON 解析失败时记录 warning（之前静默吞掉并覆盖坏文件）
  9. **Edge #14：`get_record_status_map` 缓存键加 CSV 路径**：切换审查/正常模式时不再返回旧模式的状态映射（之前 mode switch 后缓存不区分模式）
- **影响范围**：`render_control_panel`（#1、#5 导航改用筛选列表）、`start_inspection`（#2）、`save_to_disk`（#3 TXT 原子写）、`_save_df_at`（#4 os.replace）、`render_confirm_panel`（#9 跳过未纳入标注员）、`_save_tags_to_disk`（#10 异常保护）、`utils.py`（#11 logging.warning + import logging）、`get_record_status_map` + `_get_active_csv_path` + `load_record_df`（#14 缓存键区分模式）
- **备注**：本次修复覆盖 4 个真 Bug（其中 2 个会导致直接崩溃）和 5 个可复现边缘情况。审查/复核、标签删除等核心功能回归正常；原子写入保护数据完整性；筛选导航修复提升交互一致性

---

## 2026-05-21 11:15

### Bug修复 — 大图内存溢出保护

- **改动内容**：
  1. **`_load_and_rotate` 加载时限制最大边长**：新增 `MAX_LOAD_PX = 3840`，旋转后若最长边超过 3840px 则等比缩放到 4K，LRU 缓存不再存全分辨率原图（一张 8000×6000 的图从 ~144MB 降到 ~33MB）
  2. **解压炸弹保护**：设置 `Image.MAX_IMAGE_PIXELS` 和 `img.verify()` 预检，防止超大像素图片直接崩 PIL
  3. **高清大图弹窗加 4K 限制**：`show_large_image` 在 `st.image()` 前强制缩到最长边 3840px，与原图 50MB 直接传给浏览器→崩 告别
  4. **图片加载失败兜底**：`_load_and_rotate` 返回 None 时，`get_display_image_bytes` 返回灰色占位图而非崩
- **影响范围**：`utils.py`（`_load_and_rotate` 加尺寸限制 + 解压保护 + 新增 `MAX_LOAD_PX` 常量）、`app.py`（`show_large_image` 加 4K 限制 + None 兜底、`get_display_image_bytes` 加 None 兜底）
- **备注**：彻底解决了大尺寸图片（几十 MB 原图）导致的 MemoryError 崩溃。4K 限制对实际使用无影响——4K 显示器像素密度已远超肉眼分辨极限，质检够用

---

## 2026-05-21 11:30

### Bug修复 — Windows CSV PermissionError 根治

- **改动内容**：
  1. **`save_to_disk` 改用 `_get_df()` 替代直接 `pd.read_csv()`**：`_get_df` 走内存缓存（`st.session_state`），第一遍保存后缓存已存在，后续保存不再读盘，从根本上消除 Windows 文件锁冲突
  2. **`_get_df` 读盘路径加 3 次重试**：缓存 miss 需读盘时，`pd.read_csv()` 也加 PermissionError 重试（0.1s/0.2s/0.3s 退避），双重兜底
- **影响范围**：`app.py`（`save_to_disk` 方法、`_get_df` 方法）
- **备注**：此前只在 `_save_df` 写路径加了重试，但 PermissionError 实际来源于 `save_to_disk` 中的 `pd.read_csv()` 读操作（Windows 杀毒/索引服务间歇锁文件）。改为走缓存后，复审第二个条目时零磁盘读取，彻底解决问题

---

## 2026-05-21 当前（第二轮修复）

### Bug修复 — save_to_disk / _save_df 还原至 1.0.8 行为

- **改动内容**：通过与 1.0.8（无此 bug）逐行对比，定位到三个差异点并全部还原：
  1. **`save_to_disk` 恢复直读 CSV**：`self._get_df(csv_file)`（走缓存层）→ `pd.read_csv(csv_file, dtype=str)`（直读磁盘），与 1.0.8 一致。写后增加 `st.session_state.pop(_df_cache_)` 和 `st.session_state.pop(_df_lookup_)` 清除缓存
  2. **`_save_df` 恢复备份 + 移除 fsync**：恢复 `os.replace(csv→bak)` 备份 + `os.replace(tmp→csv)` 两步原子写入；移除 `_fsync_path` 调用（`os.open/O_RDONLY + os.fsync + os.close`），因为该操作在 Windows 上可能触发杀毒软件异步扫描持锁，导致下一条保存时 `os.replace` 失败
  3. **移除重试逻辑**：还原为单次 `os.replace`（无重试/无退避），与 1.0.8 一致
- **影响范围**：`app.py` `_save_df` 方法（L349-369）、`save_to_disk` 方法（L935-977）
- **备注**：此前两次尝试（11:30 给 `_get_df` 加重试、当前给 `_save_df` 去备份加重试）均未解决。根因是 1.1.0 相比 1.0.8 多了 `_fsync_path` 调用——Windows 上 `os.open()+os.fsync()` 可能激活杀毒软件异步扫描，文件在 `os.close()` 后被短暂持锁。去掉 fsync 并恢复 1.0.8 的完整行为模式

---

## 2026-05-21 当前

### 新增功能 — 自定义读取规则 (v1.1.1)

- **改动内容**：
  1. **配置持久化**：新增 `scan_rules.json` 配置文件（位于 `BASE_DIR`），支持自定义文件夹位数、图片槽位后缀、文本槽位后缀
  2. **配置读写函数**：`utils.py` 新增 `DEFAULT_SCAN_RULES` 常量、`SCAN_RULES_FILENAME`、`get_scan_rules_path()`、`load_scan_rules()`（带损坏回退默认逻辑）、`save_scan_rules()`
  3. **`extract_user_name` 参数化**：新增 `digit_length=8` 参数，从硬编码 8 位数字判断改为可配置位数
  4. **`scan_files_from_disk` 重构**：新增 `_rules_json` 参数，文件夹过滤、图片匹配（多槽位 + stem_suffixes）、TXT 匹配（多槽位 + suffixes）全部从配置读取，默认值与 1.1.0 硬编码行为完全一致（向后兼容）
  5. **`_build_group_from_path` 同步适配**：从 `st.session_state.scan_rules` 读取配置，应用相同的多槽位图片/TXT 匹配逻辑
  6. **UI 面板**：新增 `_render_scan_rules_panel()` 方法，在「更多设置」中渲染自定义规则面板（文件夹位数 number_input、图片槽位 text_input 列表 + −/添加、文本槽位 text_input 列表 + −/添加、💾 保存规则 + 🔄 恢复默认）
  7. **生命周期集成**：`__init__` 中初始化 `scan_rules` session_state（`load_scan_rules()`）；所有 `scan_files_from_disk` 调用传入 `_rules_json`；面板在两处「更多设置」expander 中渲染（已加载 + 冷启动）
  8. **标题更新**：V1.1.0 → V1.1.1
- **影响范围**：`utils.py`（新增常量 + 3 个函数 + `extract_user_name` 签名变更）、`app.py`（import 更新、`__init__` 新增 `scan_rules` 初始化、`scan_files_from_disk` 签名 + 逻辑重构、`_build_group_from_path` 逻辑重构、新增 `_render_scan_rules_panel`、`run()` 两处面板插入 + 扫描调用传参、`render_inspection_panel` 扫描调用传参、标题更新）
- **备注**：默认配置与 1.1.0 硬编码行为完全一致，无任何 breaking change。配置损坏时自动回退默认并写回。`reset_task_state` 不清理 `scan_rules`——切换数据集时保留扫描规则

---

## 2026-05-21 当前（Bug修复）

### Bug修复 — 自定义读取规则 3 项缺陷修复

- **改动内容**：
  1. **文件夹位数不生效**：`scan_files_from_disk` 的 `_rules_json` 参数有 `_` 前缀，Streamlit `@st.cache_data` 会忽略它作为缓存键，导致改了规则后缓存不刷新。修复：`_rules_json` → `rules_json`（去掉下划线），所有调用点同步更新
  2. **删除/添加槽位按钮无效**：面板中增删按钮只修改了局部 Python 变量（`new_image_slots` / `new_text_slots`），`st.rerun()` 后局部变量丢失，状态回到已保存规则。修复：改用 `st.session_state._sr_working` 作为可变工作副本，增删按钮直接修改 session_state 中的列表（`on_click` 时修改后 rerun，session_state 持久保留）
  3. **重置数据集时面板状态残留**：`reset_task_state` 前缀列表增加 `"_sr_"`，切换数据集时清理扫描规则面板的临时工作副本和输入框 key，下次打开面板自动从已保存规则重新初始化
  4. **UI 文案优化**：图片槽位 label 从"后缀列表（逗号分隔）"改为"名称后缀，逗号分隔"；help 改为"仅填写文件名的后缀部分（不包含 .jpg），如 _result。空=直接匹配 {id}.jpg"；文本框 label + help 同步优化；面板底部增加匹配规则说明
- **影响范围**：`app.py` `scan_files_from_disk` 签名（`_rules_json` → `rules_json`）、2 处调用点、`_render_scan_rules_panel` 完整重写、`reset_task_state` 前缀列表
- **备注**：根因是 `@st.cache_data` 的 `_` 前缀约定和局部变量生命周期问题

---

## 2026-05-21 当前（Bug修复 #2）

### Bug修复 — 删除槽位后 text_input key 错位

- **改动内容**：增删图片/文本槽位后，`st.text_input` 的 `key=f"_sr_img_{i}"` 因索引位移导致 key 与数据错位（删除索引 1 后，原索引 2 的数据移到索引 1，但 `_sr_img_1` key 仍缓存着旧索引 1 的值）。修复：新增 `_clear_sr_widget_keys()` 静态方法，增删操作后先清除全部 `_sr_img_*` / `_sr_txt_*` widget key，rerun 后 `value=` 参数从正确的 `_sr_working` 数据重新初始化
- **影响范围**：`app.py` `_render_scan_rules_panel` 中 4 处增删按钮处理（添加图片槽位/文本框、删除图片槽位/文本框）
- **备注**：Streamlit `st.text_input` 在第二次渲染时优先读取 key 的缓存值，忽略 `value=` 参数。清除 key 后 widget 行为等同首次渲染

---

## 2026-05-21 当前（新增功能）

### 新增功能 — `` ` `` 热键打开原图

- **改动内容**：按 `` ` `` 键（反引号）从"打开当前组文件夹"改为"打开原图"。C 区控制面板新增 `🖼️` 按钮（与 `📂` 并列，原图不存在时 disabled），JS 热键处理器改为查找并点击 `🖼️` 按钮
- **影响范围**：`app.py` — `render_control_panel`（C 区按钮布局从单按钮改为两列按钮）、`inject_hotkeys`（JS 中 `` ` `` 热键目标从 `📂 打开当前组文件夹` 改为 `🖼️`）
- **备注**：隐藏功能，不对外宣传。`📂` 按钮保留（手动点击仍可打开文件夹）

---

## 2026-05-21 当前（Bug修复 #3）

### Bug修复 — 快捷标签增删时 Streamlit session_state 修改报错

- **改动内容**：`render_tag_selector` 中 pills 选择变化后直接调用 `_sync_tags_to_notes` 修改 `st.session_state[f"feedback_{id}"]`，但此时 `st.text_area(key=f"feedback_{id}")` 已在同一次渲染中先执行，Streamlit 不允许 widget 实例化后再修改其 session_state key。修复：`render_tag_selector` 不再直接调 `_sync_tags_to_notes`，改为设置 `_pending_tag_sync_{id}` 标志位 + `st.rerun()`；实际同步逻辑移到 `render_control_panel` 顶部（在任何 widget 渲染前执行）
- **影响范围**：`app.py` — `render_control_panel`（新增 pending flag 检测 + 提前同步）、`render_tag_selector`（`_sync_tags_to_notes` 调用替换为 flag + rerun）
- **备注**：根因是 Streamlit 的 widget 实例化顺序约束——修改 session_state key 必须在对应 widget 渲染之前

---

## 2026-05-21 当前（Bug修复 #4）

### Bug修复 — 抽检模式/模式切换时状态缓存串数据 + Excel 导出读错 CSV

- **改动内容**：
  1. **`get_record_status_map` 缓存键忽略 CSV 路径**：参数 `_csv_path` 带下划线前缀，Streamlit `@st.cache_data` 会忽略它作为缓存键。验收模式和抽检模式使用不同 CSV，但缓存键相同，导致切换模式后 UI 显示的状态是旧 CSV 的数据（如主 CSV 的"合格"被当作抽检 CSV 的状态）。修复：`_csv_path` → `csv_path`（去掉下划线），不同 CSV 独立缓存
  2. **`export_excel_with_images` 抽检模式下读错 CSV**：始终调用 `_get_df()`（默认主 CSV `get_csv_filename()`），但抽检复核的保存走的是 `save_inspection_result` → `get_inspection_csv_path()`（不同文件）。抽检模式下导出的 Excel 永远是主 CSV 的旧数据，而不是最新的复核结果。修复：改用 `_get_active_csv_path()` 获取当前模式对应的 CSV 路径
- **影响范围**：`app.py` — `get_record_status_map`（`_csv_path` → `csv_path`）、`export_excel_with_images`（CSV 路径从 `get_csv_filename()` 改为 `_get_active_csv_path()`）
- **备注**：两个 bug 组合起来就是用户反馈的现象——Excel 导出显示"合格"，但 CSV 和操作时明明是"不合格"

---

## 2026-05-21 当前（Bug修复 #5）

### Bug修复 — CSV 图片ID 格式不一致导致重复行，导出数据与操作不一致

- **改动内容**：如果在 Excel 中打开过 CSV，Excel 会把 `00123456` 变成 `123456`（去前导零）。下次保存时 `df['图片ID'] == str(group['id'])` 不匹配（`"123456" != "00123456"`），导致不更新旧行而是追加新行，形成**重复行**。旧行残留"合格"，新行是"不合格"。A 区 `drop_duplicates(keep='last')` 显示"不合格"（正确），但导出 Excel 时两行都在，用户看到"合格"就以为数据错了。
  修复（三处）：
  1. **`save_to_disk`**：读 CSV 后归一化 `图片ID` 列（`strip()` 去空格），ID 匹配失败时增加重复行清理（删除同 ID 旧残留行，保留最后一条）
  2. **`save_inspection_result`**：同上逻辑
  3. **`_get_df`**：读 CSV 后归一化 `图片ID` 列 + `drop_duplicates(subset=['图片ID'], keep='last')`，确保从磁盘读到内存时就是干净的
- **影响范围**：`app.py` — `save_to_disk`（ID 归一化 + 去重）、`save_inspection_result`（同）、`_get_df`（读后归一化 + 去重）
- **备注**：这是数据权威性问题，波及用户对验收结果的信任。根因是 Excel 编辑 CSV 会破坏前导零，导致 pandas ID 匹配失败产生重复行。修复后无论 CSV 被什么工具打开过，都能正确去重和匹配

---

## 2026-05-22 当前 (v1.1.2)

### Bug修复 — 「一键导出合格数据」遗漏"修改后合格"结果

- **改动内容**：
  1. **`export_qualifies_with_check`（第 1492 行）**：`df['结果'] == '合格'` → `df['结果'].isin(["合格", "修改后合格"])`，修复导出时只筛选"合格"、遗漏"修改后合格"的 bug
  2. **`verify_exported_data`（第 1592 行）**：同步修复，校验逻辑也只匹配 `'合格'`，改为同时匹配 `"合格"` 和 `"修改后合格"`
- **影响范围**：`app.py` `export_qualifies_with_check`（L1492）、`verify_exported_data`（L1592）
- **备注**：导出确认弹窗（UI 层）显示统计时包含了修改后合格的数量（`export_count = s['qualified'] + s['modified']`），但实际执行导出的 `export_qualifies_with_check` 只筛选了 `'合格'`，弹窗说的和实际做的不一致。对比 `export_qualified_data`（L1325）一直正确使用了 `.isin(["合格", "修改后合格"])`，属于同一逻辑两处实现不一致的 bug。版本从 1.1.1 复制为 1.1.2

---

## 2026-05-25

### 新增功能 — 操作人员名称（CSV 文件隔离）

- **改动内容**：
  1. **`_sanitize_operator_name` 静态方法**：自动剔除路径非法字符 `\ / : * ? " < > |`
  2. **`_render_datasource_loader`**：在"任务根目录"上方新增"操作员名称"输入框，空值时禁用"加载文件夹"按钮并显示警告
  3. **冷启动「更多设置」expander**：新增操作员名称同步入口（与主区域实时同步）
  4. **`get_csv_filename`**：有操作员时路径变为 `{root_path}/_质检记录/{operator_name}/【完成验收】{task_name}.csv`，空时向后兼容
  5. **`get_inspection_csv_path`**：有操作员时路径变为 `{root_path}/_质检记录/{operator_name}/抽检复核记录.csv`，空时向后兼容
  6. **`_load_settings` / `_save_settings` / `__init__`**：`operator_name` 字段持久化到 `config/settings.json`
  7. **标题更新**：V1.1.1 → V1.1.2
- **影响范围**：`app.py` — `_load_settings`、`_save_settings`、`__init__`、`_render_datasource_loader`、`get_csv_filename`、`get_inspection_csv_path`、冷启动 B 区「更多设置」
- **备注**：基于「操作人员功能说明 v1.1.4」实现。不同操作员输入同一任务路径时，各自生成独立的质检记录文件，避免并发写入冲突。操作员名称为空时保持原有路径结构（向后兼容）

---

## 2026-05-26

### 版本发布 — v1.3.0 正式版

- **改动内容**：
  1. 从 v1.2.5.3 复制为 v1.3.0 发布版本
  2. 清理运行时缓存数据：`__pycache__/`、`app_error.log`、`config/settings.json`、`config/tags.json`
  3. 确保发布版本为干净状态，用户首次启动时程序会以默认值重建配置
- **影响范围**：版本目录 `1.3.0/`，缓存文件清理
- **备注**：原始版本 v1.2.5.3 保留不动，可回溯
