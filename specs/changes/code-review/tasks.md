## 1. 标签同步逻辑修复（P0）

- [x] 1.1 重构 `_sync_tags_to_notes`，增加安全检查避免覆盖用户输入 ✅ `app.py:412-456`
- [x] 1.2 增加用户手动编辑标记，检测到手动编辑后禁止自动同步 ✅ `app.py:429-432`, `app.py:2250-2252`
- [ ] 1.3 添加单元测试覆盖各种标签-备注组合场景

## 2. 提交跳转逻辑修复（P0）

- [x] 2.1 `render_control_panel` 中提交后跳转使用 `nav_ids` 替代 `all_ids` ✅ `app.py:2365`
- [x] 2.2 统一 `go_to_next_group` 和表单提交的跳转逻辑 ✅ 两者均使用 filtered_ids
- [ ] 2.3 测试筛选条件下提交跳转的正确性

## 3. 缓存内存控制（P1）

- [ ] 3.1 `get_display_image_bytes` 增加总内存上限检查（建议 100MB）
- [ ] 3.2 超过上限时清除最早的缓存项
- [ ] 3.3 考虑使用 `functools.lru_cache` 替代手动缓存

## 4. 版本号硬编码修复（P1）

- [ ] 4.1 `render_cold_start_animation` 中版本号改为动态读取
- [ ] 4.2 统一所有版本号显示来源

## 5. 分页筛选联动优化（P2）

- [ ] 5.1 切换筛选条件时保持当前页码（如果目标页存在）
- [ ] 5.2 增加筛选结果为空时的页码重置逻辑

## 6. UNC 路径处理（P2）

- [ ] 6.1 `export_qualifies_with_check` 增加 UNC 路径专门处理
- [ ] 6.2 测试 `\\server\share` 和 `\\server\share\folder` 两种场景

## 7. 快捷键兼容性（P3）

- [ ] 7.1 增加 Streamlit 版本检测
- [ ] 7.2 为不同版本提供降级方案
- [ ] 7.3 添加快捷键失效时的用户提示

## 8. 缓存失效时机（P3）

- [ ] 8.1 评估 `save_to_disk` 后缓存清除的必要性
- [ ] 8.2 考虑使用版本号标记而非直接清除

## 验证结果

| 任务 | 状态 | 备注 |
|------|------|------|
| 1.1 标签同步重构 | ✅ 已完成 | `app.py:412-456` |
| 1.2 手动编辑标记 | ✅ 已完成 | `app.py:429-432`, `app.py:2250-2252` |
| 2.1 跳转逻辑修复 | ✅ 已完成 | `app.py:2365` |
| 2.2 跳转逻辑统一 | ✅ 已完成 | 两者均使用 filtered_ids |
| 3.1 缓存内存上限 | ⬜ 待开始 | P1，计划 V1.1.4 |
| 4.1 版本号动态化 | ⬜ 待开始 | P1，计划 V1.1.4 |

**结论：P0 问题已全部修复，P1/P2/P3 问题待后续版本处理。**
