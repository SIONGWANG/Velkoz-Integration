# 任务清单

## 代码改动

- [x] **data_mixin.py** — 新增 `_scan_existing_csvs()` 方法
- [x] **data_mixin.py** — 修改 `get_csv_filename()` 支持 selected_csv 覆盖
- [x] **data_mixin.py** — `reset_task_state()` 中清除 `selected_csv`

## UI 改动

- [x] **settings_mixin.py** — 在 `_render_datasource_loader()` 中添加 CSV 选择器下拉框

## 文档

- [x] 创建 `review.md`
- [x] 更新 `openspec/README.md`
- [x] 更新 `项目更新维护日志.md`
