# 审视之眼 Pro V1.2.4 — UI/UX 优化任务清单

## P1 高优先级

### T1: 消除列表/标签选择的全局 rerun
- [ ] A 区 radio 列表选择改为 fragment 内部更新，不调用 `st.rerun(scope="app")`
- [ ] `render_tag_selector` 中去掉 `st.rerun()`，改用 session_state 标记待同步
- [ ] 筛选按钮点击改为 fragment 内部更新
- **影响文件**: `app.py:1957-1969`, `app.py:2046-2049`
- **预期收益**: 切图/选标签/筛选操作延迟降低 50%+

### T2: 重构 C 区操作流顺序
- [ ] 状态按钮(2×2)上移到导航按钮下方
- [ ] 快捷标签上移到 L1/L2 分类之后
- [ ] 备注框移到快捷标签之后、提交按钮之前
- [ ] 错误截图区域默认折叠
- **影响文件**: `app.py:2062-2325` (`render_control_panel`)
- **预期收益**: 操作步骤从「3次滚动+点击」减少为「单向滚动+点击」

### T3: 减少 st.rerun() 调用频率
- [ ] 审计全部 15 处 `st.rerun()` 调用，标记哪些可以去掉
- [ ] 标签变化、设置变更等场景改用 `@st.fragment` 局部更新
- [ ] 只在必须刷新 B 区整体时才使用 `st.rerun()`
- **影响文件**: 全 `app.py`
- **预期收益**: 页面闪烁减少，交互更流畅

## P2 中优先级

### T4: 增加快捷键覆盖
- [ ] 增加 `Q`=合格, `W`=不合格, `E`=修改后合格, `R`=待定
- [ ] 增加 `N`=跳转下一个未检项
- [ ] 增加 `S`=保存（无 Ctrl 修饰）
- [ ] 更新快捷键说明面板
- **影响文件**: `app.py:905-966` (`inject_hotkeys`), `app.py:2845-2859` (`_render_hotkeys_section`)
- **预期收益**: 高级用户操作效率提升 30%+

### T5: 优化图片缓存策略
- [ ] 缓存 key 改为不依赖 mtime（在扫描时读取 mtime 存入 group）
- [ ] 淘汰策略改为 LRU（`OrderedDict.move_to_end`）
- [ ] 预加载改为异步执行
- **影响文件**: `app.py:141-192`
- **预期收益**: 网络驱动器场景下切换速度提升

### T6: 优化 CSV 读写
- [ ] `_get_df` 缓存命中时减少 `.copy()` 开销
- [ ] `get_record_status_map` 结果增加缓存
- [ ] `save_to_disk` 后只失效相关缓存 key
- **影响文件**: `app.py:330-368`, `app.py:561-569`, `app.py:985-1075`
- **预期收益**: 减少磁盘 I/O，大 CSV 场景下性能提升

### T7: 改进截图粘贴交互
- [ ] 增加 caption 提示「截图后按 Ctrl+V 粘贴」
- [ ] 增加单张删除按钮（预览图右上角 ×）
- [ ] 达到上限后允许替换（删一张再粘贴新的）
- **影响文件**: `app.py:2105-2136`
- **预期收益**: 新手用户上手更顺畅

### T8: 优化 A 区信息密度
- [ ] 统计卡片改为单行紧凑模式（2-3 个核心指标）
- [ ] A 区底部功能面板改为 Tab 导航
- [ ] 列表区占 A 区 60%+ 高度
- **影响文件**: `app.py:1819-1989` (`render_a_zone`)
- **预期收益**: 信息层级更清晰，减少滚动

### T9: 完善 L1/L2 Sticky 行为
- [ ] L1/L2 区域增加 caption 说明 sticky 机制
- [ ] L2 清空时显示 toast 提示
- [ ] 从历史记录恢复时优先使用记录值而非 sticky 值
- **影响文件**: `app.py:2166-2190`
- **预期收益**: 避免用户误以为数据丢失

## P3 低优先级

### T10: 状态颜色体系统一
- [ ] L1/L2 pills 去掉颜色 emoji 前缀
- [ ] 筛选按钮用 outline 样式，状态按钮用 filled 样式
- [ ] AI 质检结果区域增加明显「AI」标签
- **影响文件**: `app.py:2172-2186`, `app.py:2519-2543`

### T11: 改进 Toast 和反馈消息
- [ ] 保存/提交 toast 显示当前进度 (N/Total)
- [ ] 提交后 toast 显示下一条 ID
- [ ] 导出确认面板显示目标路径和预估大小
- **影响文件**: `app.py:2313-2324`, `app.py:2766-2805`

### T12: 四宫格视图图片交互优化
- [ ] 四宫格下点击图片弹出高清大图 dialog
- [ ] 📂 按钮保持可点击大小（≥0.85rem）
- **影响文件**: `app.py:2464-2505`

### T13: 分页导航优化
- [ ] 当前选中列表项增加背景色高亮
- [ ] 总页数 > 10 时增加页码跳转输入框
- [ ] 切换页码后自动滚动列表顶部
- **影响文件**: `app.py:1894-1973`

### T14: 冷启动页面优化
- [ ] 动画高度从 600px 减至 400px
- [ ] 冷启动设置面板精简
- [ ] 标注员确认说明文字精简
- **影响文件**: `app.py:661-789`, `app.py:3132-3140`

### T15: CSS 可维护性
- [ ] 自定义 CSS 抽取到独立文件
- [ ] 减少 `!important` 使用（目标: 47→15 处以下）
- [ ] 增加 Streamlit 版本兼容性注释
- **影响文件**: `app.py:2901-2997`

### T16: 可访问性改进
- [ ] 浅灰色文字 `#999` 改为 `#666` 或更深
- [ ] 为关键按钮增加 `aria-label`
- [ ] 快捷键说明增加屏幕阅读器支持
- **影响文件**: `app.py:2901-2997`
